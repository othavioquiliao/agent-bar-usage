---
phase: 01-backend-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/config/backend-request.ts
  - apps/backend/src/core/provider-adapter.ts
  - apps/backend/src/core/provider-registry.ts
  - apps/backend/src/core/backend-coordinator.ts
  - apps/backend/src/cache/snapshot-cache.ts
  - apps/backend/src/utils/subprocess.ts
  - apps/backend/test/cache-refresh.test.ts
autonomous: true
requirements:
  - BACK-02
user_setup: []
must_haves:
  truths:
    - Refresh behavior is centralized above provider adapters and supports all-providers or single-provider execution.
    - Default reads can return cached snapshots within a short TTL, and forced refresh bypasses that cache.
    - The backend owns provider selection and failure isolation without leaking that logic into the GNOME extension.
  artifacts:
    - apps/backend/src/core/backend-coordinator.ts
    - apps/backend/src/cache/snapshot-cache.ts
    - apps/backend/src/core/provider-registry.ts
    - apps/backend/test/cache-refresh.test.ts
  key_links:
    - The backend coordinator resolves provider adapters through the registry.
    - The cache layer is consulted before adapter execution unless force refresh is requested.
---

<objective>
Implement the provider runtime, refresh coordinator, and cache policy in the Node backend.

Purpose: Make the backend cheap to poll and predictable to debug without burying TTL logic inside each provider adapter.
Output: Adapter interface, registry, coordinator, cache layer, subprocess helper, and refresh tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
@CodexBar/Sources/CodexBarCore/Providers/ProviderDescriptor.swift
@CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift
@CodexBar/Sources/CodexBarCore/Host/PTY/TTYCommandRunner.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce backend request, adapter, and registry primitives</name>
  <files>apps/backend/src/config/backend-request.ts, apps/backend/src/core/provider-adapter.ts, apps/backend/src/core/provider-registry.ts</files>
  <read_first>packages/shared-contract/src/request.ts, .planning/phases/01-backend-contract/01-CONTEXT.md, ubuntu-extension-analysis/gjs-node-v1-architecture.md</read_first>
  <action>Implement a backend request normalizer plus a provider adapter interface and central registry. The adapter interface should expose availability and fetch behavior, while the registry owns selection by provider id. Keep the source-mode concept explicit with values `auto`, `cli`, `oauth`, `api`, and `web` so future adapters do not invent incompatible enums.</action>
  <verify>sed -n '1,240p' apps/backend/src/core/provider-adapter.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/core/provider-adapter.ts` contains `isAvailable`
    - `apps/backend/src/core/provider-adapter.ts` contains `fetch`
    - `apps/backend/src/core/provider-registry.ts` contains provider lookup by id
  </acceptance_criteria>
  <done>The backend has explicit runtime primitives for provider orchestration.</done>
</task>

<task type="auto">
  <name>Task 2: Build the coordinator and short-lived snapshot cache</name>
  <files>apps/backend/src/core/backend-coordinator.ts, apps/backend/src/cache/snapshot-cache.ts, apps/backend/src/utils/subprocess.ts</files>
  <read_first>apps/backend/src/core/provider-adapter.ts, packages/shared-contract/src/request.ts, CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift</read_first>
  <action>Implement `backend-coordinator.ts` so it accepts a normalized request, resolves either one provider or all enabled providers, and returns the shared snapshot envelope. Put TTL and force-refresh behavior in `snapshot-cache.ts` with a default TTL of `30` seconds. Use `subprocess.ts` as the single boundary for CLI-backed providers such as Codex and Claude. Do not place cache semantics inside provider adapters.</action>
  <verify>sed -n '1,260p' apps/backend/src/core/backend-coordinator.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/core/backend-coordinator.ts` contains `forceRefresh`
    - `apps/backend/src/cache/snapshot-cache.ts` contains `30`
    - `apps/backend/src/core/backend-coordinator.ts` can resolve one provider or all providers
  </acceptance_criteria>
  <done>The backend can coordinate refresh behavior with centralized TTL logic.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for cache-hit and forced-refresh behavior</name>
  <files>apps/backend/test/cache-refresh.test.ts</files>
  <read_first>apps/backend/src/core/backend-coordinator.ts, apps/backend/src/cache/snapshot-cache.ts, .planning/phases/01-backend-contract/01-VALIDATION.md</read_first>
  <action>Add deterministic Vitest coverage proving that two reads within the TTL reuse cached data and that `force_refresh` triggers a new fetch. The tests must cover one-provider and all-provider execution paths and use stub adapters rather than live providers.</action>
  <verify>sed -n '1,240p' apps/backend/test/cache-refresh.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/cache-refresh.test.ts` contains `force_refresh`
    - `apps/backend/test/cache-refresh.test.ts` contains `ttl_seconds`
    - `apps/backend/test/cache-refresh.test.ts` has separate assertions for one provider and multiple providers
  </acceptance_criteria>
  <done>Refresh and cache semantics are covered by deterministic backend tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- cache-refresh`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --provider codex --json`
- [ ] A forced-refresh run produces a later `generated_at` than a cache-hit run
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend coordinator can refresh one provider or all providers with centralized TTL behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-02-SUMMARY.md`
</output>
