---
phase: 04-ubuntu-desktop-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gnome-extension/package.json
  - apps/gnome-extension/vitest.config.ts
  - apps/gnome-extension/metadata.json
  - apps/gnome-extension/extension.js
  - apps/gnome-extension/panel/indicator.js
  - apps/gnome-extension/state/extension-state.js
  - apps/gnome-extension/utils/json.js
  - apps/gnome-extension/test/extension-state.test.js
autonomous: true
requirements:
  - UI-01
user_setup: []
must_haves:
  truths:
    - The repository contains a first-class `apps/gnome-extension` workspace instead of ad-hoc shell files.
    - The extension has a valid GNOME Shell scaffold for Ubuntu 24.04.4 LTS with clean enable/disable lifecycle boundaries.
    - The extension introduces a testable pure-JS seam so later UI work does not depend on GNOME Shell imports everywhere.
  artifacts:
    - apps/gnome-extension/metadata.json
    - apps/gnome-extension/extension.js
    - apps/gnome-extension/state/extension-state.js
    - apps/gnome-extension/test/extension-state.test.js
  key_links:
    - `extension.js` delegates panel creation to `panel/indicator.js` instead of embedding UI inline.
    - `state/extension-state.js` stays free of `resource:///org/gnome/shell/*` imports so Vitest can exercise later refresh/menu logic.
---

<objective>
Scaffold the GNOME Shell extension app for Ubuntu 24.04.4 LTS and establish the architecture seam that later plans will build on.

Purpose: create a real `apps/gnome-extension` workspace with GNOME metadata, extension lifecycle, a minimal panel indicator, and pure modules that can be tested without GNOME Shell imports.
Output: extension workspace files, initial indicator lifecycle, baseline state helpers, and extension test harness.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md
@.planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
@apps/backend/package.json
@package.json
@pnpm-workspace.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the GNOME extension workspace and metadata scaffold</name>
  <files>apps/gnome-extension/package.json, apps/gnome-extension/vitest.config.ts, apps/gnome-extension/metadata.json, apps/gnome-extension/extension.js</files>
  <read_first>package.json, pnpm-workspace.yaml, .planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md, ubuntu-extension-analysis/gjs-node-v1-architecture.md</read_first>
  <action>Create `apps/gnome-extension/package.json` as a private ESM workspace package named `gnome-extension` with a `test` script that runs Vitest. Create `apps/gnome-extension/vitest.config.ts` for Node-based tests of pure modules only. Create `apps/gnome-extension/metadata.json` with a valid GNOME extension identity, a unique `uuid`, a descriptive `name`, `description`, `version`, and a `shell-version` array containing `"46"`. Create `apps/gnome-extension/extension.js` as the entrypoint that exports the default extension class, initializes `this._indicator` to `null`, creates the indicator in `enable()`, and destroys/clears it in `disable()`.</action>
  <verify>sed -n '1,240p' apps/gnome-extension/metadata.json</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/package.json` contains `"name": "gnome-extension"` and a `"test"` script
    - `apps/gnome-extension/vitest.config.ts` exists
    - `apps/gnome-extension/metadata.json` contains `"shell-version"` and `"46"`
    - `apps/gnome-extension/extension.js` contains `enable()` and `disable()`
  </acceptance_criteria>
  <done>The repository has a real GNOME extension workspace and entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal panel indicator shell and extension lifecycle handoff</name>
  <files>apps/gnome-extension/panel/indicator.js, apps/gnome-extension/extension.js</files>
  <read_first>apps/gnome-extension/extension.js, .planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md, ubuntu-extension-analysis/gjs-node-v1-architecture.md</read_first>
  <action>Create `apps/gnome-extension/panel/indicator.js` with a small GNOME Shell indicator abstraction built on `PanelMenu.Button`. The indicator must create a `St.Icon` using a symbolic icon name, expose `destroy()` cleanup, and be added to `Main.panel` by `extension.js` through `Main.panel.addToStatusArea(this.uuid, indicator)`. Update `extension.js` so `enable()` instantiates the indicator once and `disable()` destroys it safely even if initialization was partial.</action>
  <verify>sed -n '1,260p' apps/gnome-extension/panel/indicator.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/panel/indicator.js` contains `PanelMenu.Button`
    - `apps/gnome-extension/panel/indicator.js` contains `St.Icon`
    - `apps/gnome-extension/extension.js` contains `Main.panel.addToStatusArea`
    - `apps/gnome-extension/extension.js` clears `this._indicator` in `disable()`
  </acceptance_criteria>
  <done>The extension can show and clean up a minimal top-bar indicator stub.</done>
</task>

<task type="auto">
  <name>Task 3: Establish the pure-JS state seam and baseline tests</name>
  <files>apps/gnome-extension/state/extension-state.js, apps/gnome-extension/utils/json.js, apps/gnome-extension/test/extension-state.test.js</files>
  <read_first>apps/gnome-extension/package.json, apps/gnome-extension/vitest.config.ts, .planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md</read_first>
  <action>Create `apps/gnome-extension/state/extension-state.js` with exported pure functions for creating initial extension state and applying loading, success, and error transitions over a snapshot envelope. Create `apps/gnome-extension/utils/json.js` with a strict JSON parse helper that throws a useful error when backend stdout is invalid. Add `apps/gnome-extension/test/extension-state.test.js` covering initial state creation, loading transition, successful snapshot application, and invalid JSON handling without importing GNOME Shell modules.</action>
  <verify>sed -n '1,260p' apps/gnome-extension/test/extension-state.test.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/state/extension-state.js` contains `createInitialState`
    - `apps/gnome-extension/state/extension-state.js` contains a loading transition function
    - `apps/gnome-extension/test/extension-state.test.js` contains `createInitialState`
    - `apps/gnome-extension/test/extension-state.test.js` does not import `resource:///org/gnome/shell`
  </acceptance_criteria>
  <done>The extension has a testable state/core seam ready for backend integration work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter gnome-extension test`
- [ ] `test -f apps/gnome-extension/metadata.json && test -f apps/gnome-extension/extension.js`
- [ ] `apps/gnome-extension/metadata.json` contains a GNOME Shell 46 target and `apps/gnome-extension/extension.js` contains enable/disable lifecycle methods
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository contains a GNOME extension workspace with a minimal indicator shell and a testable pure-module seam
  </success_criteria>

<output>
After completion, create `.planning/phases/04-ubuntu-desktop-surface/04-01-SUMMARY.md`
</output>
