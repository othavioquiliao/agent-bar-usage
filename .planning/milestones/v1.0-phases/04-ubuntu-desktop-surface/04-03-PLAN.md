---
phase: 04-ubuntu-desktop-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - apps/gnome-extension/panel/indicator.js
  - apps/gnome-extension/panel/menu-builder.js
  - apps/gnome-extension/panel/provider-row.js
  - apps/gnome-extension/state/extension-state.js
  - apps/gnome-extension/utils/view-model.js
  - apps/gnome-extension/test/view-model.test.js
  - apps/gnome-extension/extension.js
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - The GNOME top bar shows a usable extension surface instead of only a scaffold indicator.
    - The menu exposes provider rows with usage, reset-window, updated-at, and error details.
    - Manual refresh is available from the UI and visibly reports loading and failure states.
  artifacts:
    - apps/gnome-extension/panel/menu-builder.js
    - apps/gnome-extension/panel/provider-row.js
    - apps/gnome-extension/panel/indicator.js
    - apps/gnome-extension/test/view-model.test.js
  key_links:
    - `menu-builder.js` consumes extension state/view-models rather than talking directly to subprocess code.
    - The indicator summary and provider rows both derive from normalized backend snapshots, preserving backend/frontend contract separation.
---

<objective>
Build the initial top-bar menu, detail view, and refresh interactions.

Purpose: turn the scaffolded extension and backend bridge into the first actually useful Ubuntu desktop surface for provider visibility.
Output: menu composition, provider rows, summary indicator state, manual refresh control, and UI-level state/view-model tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md
@.planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md
@.planning/phases/04-ubuntu-desktop-surface/04-01-PLAN.md
@.planning/phases/04-ubuntu-desktop-surface/04-02-PLAN.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
@apps/gnome-extension/extension.js
@apps/gnome-extension/panel/indicator.js
@apps/gnome-extension/services/polling-service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build provider-row and menu composition around normalized snapshots</name>
  <files>apps/gnome-extension/panel/provider-row.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/utils/view-model.js, apps/gnome-extension/test/view-model.test.js</files>
  <read_first>apps/gnome-extension/state/extension-state.js, apps/gnome-extension/panel/indicator.js, packages/shared-contract/src/snapshot.ts, .planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md</read_first>
  <action>Create `apps/gnome-extension/utils/view-model.js` with pure functions that map provider snapshots into row-ready values for title, status text, usage text, reset text, updated-at text, and error text. Create `apps/gnome-extension/panel/provider-row.js` to render one provider row from those values. Create `apps/gnome-extension/panel/menu-builder.js` to assemble popup sections for summary, providers, and actions using `PopupMenu` primitives. Add `apps/gnome-extension/test/view-model.test.js` covering ok, error, and unavailable provider snapshots plus empty-envelope behavior.</action>
  <verify>sed -n '1,280p' apps/gnome-extension/panel/menu-builder.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/utils/view-model.js` contains functions for usage or error text mapping
    - `apps/gnome-extension/panel/provider-row.js` exists and contains a provider row render path
    - `apps/gnome-extension/panel/menu-builder.js` contains `PopupMenu`
    - `apps/gnome-extension/test/view-model.test.js` covers ok and error snapshot cases
  </acceptance_criteria>
  <done>The extension can build provider detail rows from the normalized backend contract.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the indicator summary and detail menu for real provider visibility</name>
  <files>apps/gnome-extension/panel/indicator.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/extension.js</files>
  <read_first>apps/gnome-extension/panel/indicator.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/extension.js, apps/gnome-extension/utils/view-model.js</read_first>
  <action>Update `apps/gnome-extension/panel/indicator.js` so it owns the visible summary icon and optional label for loading, healthy, and error-heavy states. Make it consume the menu builder and rebuild menu contents when the latest extension state changes. Ensure the menu contains a summary section, one provider row per provider snapshot, and a footer section with last-updated text when data exists.</action>
  <verify>sed -n '1,320p' apps/gnome-extension/panel/indicator.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/panel/indicator.js` contains distinct render handling for loading and error states
    - `apps/gnome-extension/panel/indicator.js` delegates menu construction to `menu-builder.js`
    - the menu includes a last-updated section or label
    - provider rows are rendered from the latest snapshot envelope rather than hardcoded data
  </acceptance_criteria>
  <done>The top-bar indicator and popup menu expose real provider state in a readable layout.</done>
</task>

<task type="auto">
  <name>Task 3: Add manual refresh interaction and explicit failure/readability handling</name>
  <files>apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/panel/indicator.js, apps/gnome-extension/extension.js, apps/gnome-extension/services/polling-service.js</files>
  <read_first>apps/gnome-extension/services/polling-service.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/panel/indicator.js, .planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md</read_first>
  <action>Add a `Refresh Now` menu action that calls `refreshNow()` on the polling service, disables or visually gates repeated clicks while a refresh is running, and updates the menu with loading/error feedback. Ensure provider failure text remains visible in the row detail when the backend returns structured provider errors. Update `extension.js` so all menu action handlers and state subscriptions are disconnected during `disable()`.</action>
  <verify>sed -n '1,320p' apps/gnome-extension/panel/menu-builder.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/panel/menu-builder.js` contains `Refresh Now`
    - the refresh action calls `refreshNow()`
    - error text from provider snapshots is rendered somewhere in the menu output path
    - `apps/gnome-extension/extension.js` disconnects or destroys menu-related handlers on disable
  </acceptance_criteria>
  <done>The first usable GNOME desktop surface supports manual refresh and makes backend failures understandable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter gnome-extension test -- view-model`
- [ ] `pnpm --filter backend test`
- [ ] Manual GNOME Shell smoke: enable the extension, open the menu, confirm provider rows and `Refresh Now` appear, then disable the extension and confirm cleanup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The GNOME extension exposes provider state, detail rows, and refresh interactions through a usable top-bar surface
  </success_criteria>

<output>
After completion, create `.planning/phases/04-ubuntu-desktop-surface/04-03-SUMMARY.md`
</output>
