---
phase: 02-linux-config-secrets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/config/config-schema.ts
  - apps/backend/src/config/config-path.ts
  - apps/backend/src/config/config-loader.ts
  - apps/backend/src/config/default-config.ts
  - apps/backend/src/commands/config-command.ts
  - apps/backend/src/cli.ts
  - apps/backend/test/config-loader.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-02
user_setup: []
must_haves:
  truths:
    - The backend can load configuration from an XDG-friendly Linux path without relying on project-relative defaults.
    - Provider enablement, ordering, and preferred source mode are represented in a validated JSON config model.
    - Users can inspect or validate config from the CLI without exposing raw secret values.
  artifacts:
    - apps/backend/src/config/config-schema.ts
    - apps/backend/src/config/config-loader.ts
    - apps/backend/src/commands/config-command.ts
    - apps/backend/test/config-loader.test.ts
  key_links:
    - The config loader validates disk format before any provider runtime code consumes it.
    - The CLI exposes read-only config inspection on top of the same loader used by the runtime.
---

<objective>
Design and implement the Linux config model, persistence path, and read-only config commands for the backend.

Purpose: Establish an Ubuntu-friendly configuration layer that controls provider enablement, order, and source preferences before any real provider auth logic is added.
Output: Validated JSON config schema, XDG path resolution, config loader/defaults, `config validate`, `config dump`, and config loader tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-linux-config-secrets/02-CONTEXT.md
@.planning/phases/02-linux-config-secrets/02-RESEARCH.md
@.planning/phases/01-backend-contract/01-VERIFICATION.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the persisted config schema and Linux path policy</name>
  <files>apps/backend/src/config/config-schema.ts, apps/backend/src/config/config-path.ts, apps/backend/src/config/default-config.ts</files>
  <read_first>.planning/phases/02-linux-config-secrets/02-CONTEXT.md, .planning/phases/02-linux-config-secrets/02-RESEARCH.md, packages/shared-contract/src/request.ts</read_first>
  <action>Create a JSON config schema with `zod` that supports an ordered provider array, `enabled`, `sourceMode`, and secret references without raw secret values. Add Linux path resolution based on `${XDG_CONFIG_HOME:-~/.config}/agent-bar/config.json`. Define minimal defaults so the backend can behave predictably when the config file is missing.</action>
  <verify>sed -n '1,260p' apps/backend/src/config/config-schema.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/config/config-path.ts` contains `XDG_CONFIG_HOME`
    - `apps/backend/src/config/config-schema.ts` contains `enabled`
    - `apps/backend/src/config/config-schema.ts` contains `sourceMode`
  </acceptance_criteria>
  <done>The backend has a validated persisted config format and a single authoritative Linux path policy.</done>
</task>

<task type="auto">
  <name>Task 2: Implement config loading, defaults, and safe CLI inspection commands</name>
  <files>apps/backend/src/config/config-loader.ts, apps/backend/src/commands/config-command.ts, apps/backend/src/cli.ts</files>
  <read_first>apps/backend/src/config/config-schema.ts, apps/backend/src/config/config-path.ts, apps/backend/src/cli.ts, .planning/ROADMAP.md</read_first>
  <action>Implement a config loader that reads the XDG path, applies defaults when the file is missing, validates JSON, and returns a typed config object. Extend the CLI with `config validate` and `config dump`, ensuring `config dump` never prints raw secret material and instead shows sanitized references or redacted metadata.</action>
  <verify>sed -n '1,260p' apps/backend/src/commands/config-command.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/cli.ts` contains `config`
    - `apps/backend/src/commands/config-command.ts` contains `validate`
    - `apps/backend/src/commands/config-command.ts` contains `dump`
  </acceptance_criteria>
  <done>Users can inspect and validate backend config through stable read-only commands.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for XDG path resolution and config validation behavior</name>
  <files>apps/backend/test/config-loader.test.ts</files>
  <read_first>apps/backend/src/config/config-loader.ts, apps/backend/src/config/config-path.ts, .planning/phases/02-linux-config-secrets/02-VALIDATION.md</read_first>
  <action>Create Vitest coverage proving that the backend resolves the correct XDG config path, accepts a valid ordered provider array, rejects malformed JSON/schema violations, and falls back to defaults when the config file does not exist.</action>
  <verify>sed -n '1,240p' apps/backend/test/config-loader.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/config-loader.test.ts` contains `XDG_CONFIG_HOME`
    - `apps/backend/test/config-loader.test.ts` contains `sourceMode`
    - `apps/backend/test/config-loader.test.ts` uses `vitest`
  </acceptance_criteria>
  <done>The config layer is protected by deterministic tests before secrets are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- config-loader`
- [ ] `pnpm --filter backend exec tsx src/cli.ts config validate`
- [ ] `pnpm --filter backend exec tsx src/cli.ts config dump`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend can resolve Linux config, expose provider order/preferences, and validate config from the CLI
  </success_criteria>

<output>
After completion, create `.planning/phases/02-linux-config-secrets/02-01-SUMMARY.md`
</output>
