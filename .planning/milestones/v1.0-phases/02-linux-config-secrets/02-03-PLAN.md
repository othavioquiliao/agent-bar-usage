---
phase: 02-linux-config-secrets
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/backend/src/core/provider-context-builder.ts
  - apps/backend/src/core/backend-coordinator.ts
  - apps/backend/src/core/provider-adapter.ts
  - apps/backend/src/cli.ts
  - apps/backend/test/provider-context.test.ts
  - apps/backend/test/cache-refresh.test.ts
autonomous: true
requirements:
  - CONF-02
  - SECR-01
user_setup: []
must_haves:
  truths:
    - Provider runtime behavior now reflects persisted config order, enablement, and preferred source mode.
    - Provider execution contexts receive resolved secret material through the backend before adapter execution begins.
    - Missing secret material or disabled providers degrade predictably without collapsing unrelated providers.
  artifacts:
    - apps/backend/src/core/provider-context-builder.ts
    - apps/backend/src/core/backend-coordinator.ts
    - apps/backend/test/provider-context.test.ts
  key_links:
    - Provider selection/order is derived from config and then refined by request overrides.
    - Secret resolution is injected into provider runtime contexts rather than looked up from inside adapters.
---

<objective>
Wire config and secret resolution into the backend runtime so provider contexts reflect persisted Linux settings.

Purpose: Make the backend actually honor provider ordering, enablement, preferred source mode, and secret material before Phase 3 introduces real provider implementations.
Output: Provider context builder, config-aware coordinator integration, runtime precedence rules, and provider context tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-linux-config-secrets/02-CONTEXT.md
@.planning/phases/02-linux-config-secrets/02-RESEARCH.md
@.planning/phases/02-linux-config-secrets/02-01-PLAN.md
@.planning/phases/02-linux-config-secrets/02-02-PLAN.md
@.planning/phases/01-backend-contract/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build provider-context resolution from config, request overrides, and secret references</name>
  <files>apps/backend/src/core/provider-context-builder.ts, apps/backend/src/core/provider-adapter.ts</files>
  <read_first>apps/backend/src/config/config-loader.ts, apps/backend/src/secrets/secret-store.ts, apps/backend/src/core/provider-adapter.ts, .planning/phases/02-linux-config-secrets/02-RESEARCH.md</read_first>
  <action>Create a provider context builder that derives the effective provider list and per-provider source mode from config, then applies explicit request overrides. Extend the provider context shape so adapters can receive resolved secret material and provider config metadata without directly reading files or environment variables.</action>
  <verify>sed -n '1,260p' apps/backend/src/core/provider-context-builder.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/core/provider-context-builder.ts` contains provider ordering logic
    - `apps/backend/src/core/provider-adapter.ts` contains secret-aware runtime context fields
    - Request overrides remain explicit and testable
  </acceptance_criteria>
  <done>The backend has one place that computes effective provider execution context.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate effective config and secret resolution into the coordinator and CLI</name>
  <files>apps/backend/src/core/backend-coordinator.ts, apps/backend/src/cli.ts</files>
  <read_first>apps/backend/src/core/provider-context-builder.ts, apps/backend/src/core/backend-coordinator.ts, apps/backend/src/cli.ts, .planning/phases/02-linux-config-secrets/02-01-PLAN.md, .planning/phases/02-linux-config-secrets/02-02-PLAN.md</read_first>
  <action>Update the coordinator and CLI so `usage` requests resolve effective config first, skip disabled providers by default, honor config-defined provider order, and inject resolved secret data into adapter contexts. Keep provider failures isolated and preserve the normalized output contract introduced in Phase 1.</action>
  <verify>sed -n '1,260p' apps/backend/src/core/backend-coordinator.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/core/backend-coordinator.ts` uses config-derived provider selection
    - `apps/backend/src/cli.ts` initializes runtime services for config and secret resolution
    - Disabled providers are not fetched unless an explicit override contract says otherwise
  </acceptance_criteria>
  <done>The public backend behavior now reflects persisted config and secret resolution.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime tests for provider ordering, source precedence, and secret-aware contexts</name>
  <files>apps/backend/test/provider-context.test.ts, apps/backend/test/cache-refresh.test.ts</files>
  <read_first>apps/backend/src/core/provider-context-builder.ts, apps/backend/src/core/backend-coordinator.ts, .planning/phases/02-linux-config-secrets/02-VALIDATION.md</read_first>
  <action>Create Vitest coverage proving that config order is preserved, disabled providers are skipped, explicit request overrides win over config defaults where intended, and resolved secret material reaches the provider runtime context without leaking into serialized output. Extend cache tests only where config-aware selection changes runtime expectations.</action>
  <verify>sed -n '1,260p' apps/backend/test/provider-context.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/provider-context.test.ts` contains provider order assertions
    - `apps/backend/test/provider-context.test.ts` contains source-mode precedence assertions
    - Serialized output tests confirm secrets do not leak into snapshots
  </acceptance_criteria>
  <done>Config-driven runtime behavior is protected before real provider implementations land.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- provider-context cache-refresh`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --json`
- [ ] Config-defined provider order is reflected in output for a sample config
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Backend runtime behavior now respects persisted config, source preference, and secret resolution
  </success_criteria>

<output>
After completion, create `.planning/phases/02-linux-config-secrets/02-03-SUMMARY.md`
</output>
