---
phase: 02-linux-config-secrets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/secrets/secret-reference.ts
  - apps/backend/src/secrets/secret-store.ts
  - apps/backend/src/secrets/secret-tool-store.ts
  - apps/backend/src/secrets/env-secret-store.ts
  - apps/backend/src/utils/subprocess.ts
  - apps/backend/test/secret-store.test.ts
autonomous: true
requirements:
  - SECR-01
user_setup: []
must_haves:
  truths:
    - The backend resolves secret material through a Linux-appropriate abstraction rather than through plain-text config fields.
    - `secret-tool` is the primary persistent secret boundary, with env fallback reserved for development and CI cases.
    - Secret store failures do not force providers to invent their own secret lookup logic.
  artifacts:
    - apps/backend/src/secrets/secret-store.ts
    - apps/backend/src/secrets/secret-tool-store.ts
    - apps/backend/src/secrets/env-secret-store.ts
    - apps/backend/test/secret-store.test.ts
  key_links:
    - Secret resolution is abstracted behind one backend interface.
    - The `secret-tool` implementation uses the subprocess boundary instead of shelling out ad hoc from providers.
---

<objective>
Introduce the backend secret-store abstraction and Linux secret resolution strategy for Ubuntu.

Purpose: Give the backend a supported secret retrieval path before real provider adapters begin depending on credentials or session material.
Output: Secret reference model, store interface, `secret-tool` implementation, env fallback, and deterministic secret-store tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-linux-config-secrets/02-CONTEXT.md
@.planning/phases/02-linux-config-secrets/02-RESEARCH.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the secret reference model and store interface</name>
  <files>apps/backend/src/secrets/secret-reference.ts, apps/backend/src/secrets/secret-store.ts</files>
  <read_first>.planning/phases/02-linux-config-secrets/02-CONTEXT.md, .planning/phases/02-linux-config-secrets/02-RESEARCH.md, apps/backend/src/config/config-schema.ts</read_first>
  <action>Define a typed secret reference model that the config layer can use without embedding raw values. Add a store interface that supports resolving secret references and exposes structured failure semantics for missing stores, missing secrets, and unsupported reference types.</action>
  <verify>sed -n '1,240p' apps/backend/src/secrets/secret-store.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/secrets/secret-reference.ts` contains a secret reference type
    - `apps/backend/src/secrets/secret-store.ts` contains `resolve`
    - Secret failures are represented as structured backend errors rather than generic strings
  </acceptance_criteria>
  <done>The backend has a stable abstraction for secret references and secret resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `secret-tool` and environment-backed secret stores</name>
  <files>apps/backend/src/secrets/secret-tool-store.ts, apps/backend/src/secrets/env-secret-store.ts, apps/backend/src/utils/subprocess.ts</files>
  <read_first>apps/backend/src/secrets/secret-store.ts, apps/backend/src/utils/subprocess.ts, .planning/phases/02-linux-config-secrets/02-RESEARCH.md</read_first>
  <action>Implement a `secret-tool` store that shells out through the shared subprocess helper and an env-backed fallback store for development and CI. Keep store selection explicit and do not silently downgrade persistent references to env variables unless the reference or runtime policy allows it.</action>
  <verify>sed -n '1,260p' apps/backend/src/secrets/secret-tool-store.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/secrets/secret-tool-store.ts` contains `secret-tool`
    - `apps/backend/src/secrets/env-secret-store.ts` reads from `process.env` through injected env
    - Secret store selection remains separate from provider adapter code
  </acceptance_criteria>
  <done>The backend can resolve secrets through Ubuntu-friendly and CI-friendly paths.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for secret resolution and failure cases</name>
  <files>apps/backend/test/secret-store.test.ts</files>
  <read_first>apps/backend/src/secrets/secret-tool-store.ts, apps/backend/src/secrets/env-secret-store.ts, .planning/phases/02-linux-config-secrets/02-VALIDATION.md</read_first>
  <action>Create Vitest coverage that proves env-backed resolution works, `secret-tool` invocation is formed correctly, missing secrets return structured failures, and unsupported secret-store conditions can be surfaced without touching a real desktop keyring.</action>
  <verify>sed -n '1,240p' apps/backend/test/secret-store.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/secret-store.test.ts` contains `secret-tool`
    - `apps/backend/test/secret-store.test.ts` covers missing-secret behavior
    - `apps/backend/test/secret-store.test.ts` uses `vitest`
  </acceptance_criteria>
  <done>Linux secret handling is covered by deterministic tests before it is wired into provider contexts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- secret-store`
- [ ] Secret-store tests prove a structured failure for missing secrets
- [ ] No test depends on a live desktop keyring
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend can resolve secret references through a Linux-appropriate secret-store abstraction
  </success_criteria>

<output>
After completion, create `.planning/phases/02-linux-config-secrets/02-02-SUMMARY.md`
</output>
