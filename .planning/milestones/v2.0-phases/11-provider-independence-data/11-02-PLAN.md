---
phase: 11-provider-independence-data
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/backend/src/cache/snapshot-cache.ts
  - apps/backend/src/cache/cache-path.ts
  - apps/backend/src/core/usage-snapshot.ts
  - apps/backend/src/core/backend-coordinator.ts
  - apps/backend/src/service/service-server.ts
  - apps/backend/src/config/default-config.ts
  - apps/backend/test/cache-refresh.test.ts
  - apps/backend/test/file-snapshot-cache.test.ts
  - apps/backend/test/service-runtime.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "Snapshot cache persists to XDG_CACHE_HOME with TTL metadata"
    - "Concurrent fetches for the same provider key deduplicate behind one in-flight promise"
    - "Service runtime refreshes periodically in the background and serves cached data immediately after restart"
  artifacts:
    - path: "apps/backend/src/cache/snapshot-cache.ts"
      provides: "File-backed cache with dedup support"
    - path: "apps/backend/src/service/service-server.ts"
      provides: "Auto-refreshing service runtime with startup cache hydration"
  key_links:
    - from: "apps/backend/src/service/service-server.ts"
      to: "apps/backend/src/cache/snapshot-cache.ts"
      via: "hydrates and refreshes snapshots through persistent cache"
      pattern: "refreshInterval"
---

<objective>
Replace ephemeral in-memory caching with a persistent file-backed cache and move periodic refresh responsibility into the backend service runtime.

Purpose: This delivers the data half of Phase 11 — restart-safe cached snapshots, TTL persistence, and background refresh independent of GNOME polling.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-provider-independence-data/11-CONTEXT.md
@.planning/phases/11-provider-independence-data/11-RESEARCH.md
@.planning/phases/11-provider-independence-data/11-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a file-backed snapshot cache with TTL and in-flight dedup</name>
  <files>
    apps/backend/src/cache/snapshot-cache.ts
    apps/backend/src/cache/cache-path.ts
    apps/backend/src/core/usage-snapshot.ts
    apps/backend/src/core/backend-coordinator.ts
    apps/backend/test/cache-refresh.test.ts
    apps/backend/test/file-snapshot-cache.test.ts
  </files>
  <read_first>
    apps/backend/src/cache/snapshot-cache.ts
    apps/backend/src/core/backend-coordinator.ts
    apps/backend/src/core/usage-snapshot.ts
    /home/othavio/Work/agent-bar-omarchy/src/cache.ts
  </read_first>
  <action>
    Rework `SnapshotCache` so it stores provider snapshots on disk under a safe cache path, tracks expiry metadata, and deduplicates concurrent fetches per cache key. Thread the persistent cache through `createUsageSnapshot()` and `BackendCoordinator` so CLI and service calls share the same cache behavior.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun run vitest run test/cache-refresh.test.ts test/file-snapshot-cache.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - cache files land under XDG cache path
    - cache survives process recreation
    - dedup test proves only one fetch happens for concurrent misses
  </acceptance_criteria>
  <done>Backend snapshot caching is persistent, TTL-aware, and deduplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Teach the service runtime to auto-refresh and serve cached startup data</name>
  <files>
    apps/backend/src/service/service-server.ts
    apps/backend/src/config/default-config.ts
    apps/backend/test/service-runtime.test.ts
  </files>
  <read_first>
    apps/backend/src/service/service-server.ts
    apps/backend/src/core/usage-snapshot.ts
    apps/backend/src/config/default-config.ts
    apps/backend/test/service-runtime.test.ts
  </read_first>
  <action>
    Add a backend-owned refresh loop (default 150s, configurable via config defaults) and hydrate the service's `lastSnapshot` from persistent cache on start so `snapshot` requests can return immediately while a fresh refresh runs in the background.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun test test/service-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - service starts with last cached snapshot when available
    - refresh interval defaults to 150 seconds
    - refresh loop updates cached snapshot without requiring an extension poll-triggered refresh
  </acceptance_criteria>
  <done>Backend service owns periodic refresh and restart-safe cached startup behavior.</done>
</task>

</tasks>
