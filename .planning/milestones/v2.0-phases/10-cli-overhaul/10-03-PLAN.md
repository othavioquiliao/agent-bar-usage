---
phase: 10-cli-overhaul
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - package.json
  - apps/backend/package.json
  - biome.json
  - bun.lock
  - pnpm-lock.yaml
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "Biome is configured for the repo with 120-column single-quote formatting"
    - "Commander and Zod are removed from backend dependencies"
    - "`bun x biome check` runs against the repo and passes"
  artifacts:
    - path: "biome.json"
      provides: "Repo-wide Biome formatter/linter configuration"
    - path: "apps/backend/package.json"
      provides: "Dependency cleanup after Commander/Zod removal"
  key_links:
    - from: "package.json"
      to: "biome.json"
      via: "scripts invoke biome check/format"
      pattern: "biome"
---

<objective>
Adopt Biome repo-wide and clean dependency metadata after the Commander/Zod refactor.

Purpose: This finishes Phase 10 by enforcing the new formatting/linting baseline and proving the codebase passes Biome checks.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-cli-overhaul/10-CONTEXT.md
@.planning/phases/10-cli-overhaul/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Biome config and scripts</name>
  <files>
    package.json
    biome.json
  </files>
  <read_first>
    package.json
    /home/othavio/Work/agent-bar-omarchy/biome.json
    .gitignore
  </read_first>
  <action>
    Add `@biomejs/biome` as a root devDependency, create a root `biome.json` modeled on omarchy's configuration, and add repo-level scripts for `biome check` and `biome format`. Scope includes the backend, shared-contract package, and GNOME extension JS/TS sources where appropriate.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage && bun x biome check .</automated>
  </verify>
  <acceptance_criteria>
    - `biome.json` exists and includes `lineWidth` set to `120`
    - `biome.json` configures single quotes for JavaScript/TypeScript formatting
    - `package.json` contains scripts for Biome check/format
  </acceptance_criteria>
  <done>Biome is configured as the repo formatter/linter with runnable root scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Remove obsolete dependencies and refresh lockfiles</name>
  <files>
    apps/backend/package.json
    bun.lock
    pnpm-lock.yaml
  </files>
  <read_first>
    apps/backend/package.json
    bun.lock
    pnpm-lock.yaml
    biome.json
  </read_first>
  <action>
    Remove `commander` and `zod` from backend dependencies, install/update Biome in the workspace, and refresh lockfiles so the dependency graph matches the Phase 10 code changes. Run the final backend test subset plus Biome check after dependency cleanup.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage && bun x biome check . && cd apps/backend && bun run vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"commander\"|\"zod\"" apps/backend/package.json` returns no matches
    - `bun.lock` no longer retains active backend entries for Commander or Zod
    - `bun x biome check .` exits successfully
  </acceptance_criteria>
  <done>The repo uses Biome, lockfiles are current, and backend dependencies no longer include Commander or Zod.</done>
</task>

</tasks>
