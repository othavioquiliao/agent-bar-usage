---
phase: 10-cli-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-contract/src/request.ts
  - packages/shared-contract/src/snapshot.ts
  - packages/shared-contract/src/diagnostics.ts
  - apps/backend/src/config/config-schema.ts
  - apps/backend/src/config/config-loader.ts
  - apps/backend/src/config/backend-request.ts
  - apps/backend/src/core/backend-coordinator.ts
  - apps/backend/src/serializers/snapshot-serializer.ts
  - apps/backend/src/service/service-client.ts
  - apps/backend/src/commands/diagnostics-command.ts
  - apps/backend/test/config-loader.test.ts
  - apps/backend/test/contract.test.ts
  - apps/backend/test/output-parity.test.ts
  - apps/backend/test/prerequisite-checks.test.ts
  - apps/backend/test/service-runtime.test.ts
autonomous: true
requirements: [CLI-02]

must_haves:
  truths:
    - "External request/config/snapshot payloads are validated without Zod"
    - "Invalid config and snapshot data still throw clear runtime errors"
    - "Shared-contract exports inline guards/assertions that replace prior schema.parse usage"
  artifacts:
    - path: "packages/shared-contract/src/request.ts"
      provides: "Provider/source enums, request guards, and request normalizers"
    - path: "packages/shared-contract/src/snapshot.ts"
      provides: "Snapshot types plus assertion helpers for provider/snapshot envelopes"
    - path: "apps/backend/src/config/config-loader.ts"
      provides: "Config parsing/error handling without ZodError"
  key_links:
    - from: "apps/backend/src/config/backend-request.ts"
      to: "packages/shared-contract/src/request.ts"
      via: "imports request assertion/normalization helpers"
      pattern: "request"
    - from: "apps/backend/src/service/service-client.ts"
      to: "packages/shared-contract/src/snapshot.ts"
      via: "asserts service snapshot payloads"
      pattern: "snapshot"
---

<objective>
Remove Zod from the shared contract and backend config layers while preserving strict runtime validation and defaulting behavior.

Purpose: Phase 10 requires inline guards and assertions instead of schema objects. The rest of the CLI overhaul depends on these helpers existing first.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-cli-overhaul/10-CONTEXT.md
@.planning/phases/10-cli-overhaul/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace shared-contract Zod schemas with inline runtime helpers</name>
  <files>
    packages/shared-contract/src/request.ts
    packages/shared-contract/src/snapshot.ts
    packages/shared-contract/src/diagnostics.ts
  </files>
  <read_first>
    packages/shared-contract/src/request.ts
    packages/shared-contract/src/snapshot.ts
    packages/shared-contract/src/diagnostics.ts
    apps/backend/test/contract.test.ts
    apps/backend/test/prerequisite-checks.test.ts
  </read_first>
  <action>
    Replace Zod schema exports with literal arrays, type aliases, `isX()` guards, `assertX()` functions, and normalizers. Preserve the existing public types (`ProviderId`, `ProviderSourceMode`, `SnapshotEnvelope`, `DiagnosticsReport`) and add assertion helpers that throw `TypeError` or repo-specific `Error` messages when input is invalid. Keep strict-object semantics: reject unknown fields for request/config-facing payloads and preserve default values previously supplied by Zod defaults.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun run vitest run test/contract.test.ts test/prerequisite-checks.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared-contract/src/request.ts` contains `isProviderId` and `assertBackendUsageRequest`
    - `packages/shared-contract/src/snapshot.ts` contains `assertSnapshotEnvelope`
    - `packages/shared-contract/src/diagnostics.ts` contains `assertDiagnosticsReport`
    - `rg -n "from \"zod\"" packages/shared-contract/src` returns no matches
  </acceptance_criteria>
  <done>Shared-contract runtime validation no longer depends on Zod and all shared types remain validated by inline helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate backend config and snapshot callers to the new assertions</name>
  <files>
    apps/backend/src/config/config-schema.ts
    apps/backend/src/config/config-loader.ts
    apps/backend/src/config/backend-request.ts
    apps/backend/src/core/backend-coordinator.ts
    apps/backend/src/serializers/snapshot-serializer.ts
    apps/backend/src/service/service-client.ts
    apps/backend/src/commands/diagnostics-command.ts
    apps/backend/test/config-loader.test.ts
    apps/backend/test/output-parity.test.ts
    apps/backend/test/service-runtime.test.ts
  </files>
  <read_first>
    apps/backend/src/config/config-schema.ts
    apps/backend/src/config/config-loader.ts
    apps/backend/src/config/backend-request.ts
    apps/backend/src/core/backend-coordinator.ts
    apps/backend/src/serializers/snapshot-serializer.ts
    apps/backend/src/service/service-client.ts
    packages/shared-contract/src/request.ts
    packages/shared-contract/src/snapshot.ts
    packages/shared-contract/src/diagnostics.ts
  </read_first>
  <action>
    Replace `.parse()` and `ZodError` handling with the new assertion helpers. Keep `ConfigLoadError` codes unchanged and preserve current duplicate-provider validation plus file-aware defaults. Ensure service-client, serializers, diagnostics-command, and snapshot-building code all validate the same payloads they validated before, but via inline helpers.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun run vitest run test/config-loader.test.ts test/output-parity.test.ts test/service-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/config/config-loader.ts` contains no `ZodError` import
    - `apps/backend/src/service/service-client.ts` uses `assertSnapshotEnvelope`
    - `apps/backend/src/commands/diagnostics-command.ts` uses `assertDiagnosticsReport`
    - `rg -n "parse\\(" apps/backend/src packages/shared-contract/src | rg "snapshotEnvelopeSchema|backendUsageRequestSchema|diagnosticsReportSchema"` returns no matches
  </acceptance_criteria>
  <done>Backend callers validate runtime data through inline assertions with unchanged outward behavior.</done>
</task>

</tasks>
