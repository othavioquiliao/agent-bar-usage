---
phase: 10-cli-overhaul
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/backend/src/cli.ts
  - apps/backend/src/commands/auth-command.ts
  - apps/backend/src/commands/config-command.ts
  - apps/backend/src/commands/diagnostics-command.ts
  - apps/backend/src/commands/service-command.ts
  - apps/backend/src/commands/lifecycle-command.ts
  - apps/backend/test/cli.test.ts
autonomous: true
requirements: [CLI-01, CLI-03]

must_haves:
  truths:
    - "CLI uses manual switch/case parsing without Commander"
    - "Unknown commands suggest the nearest valid command when close enough"
    - "`agent-bar --help` renders a boxed help screen listing the available commands"
    - "Nested commands like `auth copilot`, `config validate`, and `service status` still dispatch correctly"
  artifacts:
    - path: "apps/backend/src/cli.ts"
      provides: "Manual parser, help rendering, and command dispatch"
    - path: "apps/backend/test/cli.test.ts"
      provides: "Regression coverage for parsing, help, and typo suggestions"
  key_links:
    - from: "apps/backend/src/cli.ts"
      to: "apps/backend/src/commands/*.ts"
      via: "dispatches direct runner exports instead of Commander registration"
      pattern: "run"
---

<objective>
Rewrite the backend CLI around manual parsing and direct command dispatch, mirroring the omarchy CLI ergonomics while preserving the existing command surface.

Purpose: This delivers the user-facing half of Phase 10: no Commander, typo suggestions, and a real help screen.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-cli-overhaul/10-CONTEXT.md
@.planning/phases/10-cli-overhaul/10-RESEARCH.md
@.planning/phases/10-cli-overhaul/10-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor command modules into direct runners with no Commander dependency</name>
  <files>
    apps/backend/src/commands/auth-command.ts
    apps/backend/src/commands/config-command.ts
    apps/backend/src/commands/diagnostics-command.ts
    apps/backend/src/commands/service-command.ts
    apps/backend/src/commands/lifecycle-command.ts
  </files>
  <read_first>
    apps/backend/src/commands/auth-command.ts
    apps/backend/src/commands/config-command.ts
    apps/backend/src/commands/diagnostics-command.ts
    apps/backend/src/commands/service-command.ts
    apps/backend/src/commands/lifecycle-command.ts
  </read_first>
  <action>
    Remove Commander imports and registration-only APIs. Keep or add direct runner functions so `cli.ts` can call command logic without a framework. Preserve current error formatting, stdout/stderr behavior, and exported runners already used in tests.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "commander" apps/backend/src/commands apps/backend/src/cli.ts` returns no matches
    - `apps/backend/src/commands/config-command.ts` exports direct runtime helpers for `validate` and `dump`
    - `apps/backend/src/commands/service-command.ts` exports a direct `run` helper for `service run`
  </acceptance_criteria>
  <done>All command modules are callable directly from the manual parser and no longer import Commander.</done>
</task>

<task type="auto">
  <name>Task 2: Implement manual CLI parsing, help UI, and typo suggestions</name>
  <files>
    apps/backend/src/cli.ts
    apps/backend/test/cli.test.ts
  </files>
  <read_first>
    apps/backend/src/cli.ts
    /home/othavio/Work/agent-bar-omarchy/src/cli.ts
    apps/backend/src/commands/auth-command.ts
    apps/backend/src/commands/config-command.ts
    apps/backend/src/commands/diagnostics-command.ts
    apps/backend/src/commands/service-command.ts
    apps/backend/src/commands/lifecycle-command.ts
  </read_first>
  <action>
    Replace Commander usage in `apps/backend/src/cli.ts` with a small parser/dispatcher inspired by omarchy: `requireNextArg`, Levenshtein-based `suggestCommand`, boxed `showHelp`, and switch/case parsing for top-level plus nested subcommands. Make no-arg behavior explicit and preserve `runUsageCommand` as the underlying usage implementation. Add dedicated tests covering help rendering, close-command suggestions, and nested dispatch for auth/config/service/lifecycle commands.
  </action>
  <verify>
    <automated>cd /home/othavio/Work/agent-bar-usage/apps/backend && bun run vitest run test/cli.test.ts test/contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/backend/src/cli.ts` contains `levenshtein`, `suggestCommand`, and `showHelp`
    - `apps/backend/src/cli.ts` contains no `new Command(`
    - `apps/backend/test/cli.test.ts` verifies typo suggestion for a misspelled command
    - `apps/backend/test/cli.test.ts` verifies help output contains box-drawing characters
  </acceptance_criteria>
  <done>The backend CLI parses commands manually, renders formatted help, and suggests close command matches without Commander.</done>
</task>

</tasks>
