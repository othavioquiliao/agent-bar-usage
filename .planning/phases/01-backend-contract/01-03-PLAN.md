---
phase: 01-backend-contract
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/backend/src/cli.ts
  - apps/backend/src/formatters/text-formatter.ts
  - apps/backend/src/serializers/snapshot-serializer.ts
  - apps/backend/test/output-parity.test.ts
  - apps/backend/test/snapshot-mapping.test.ts
  - apps/backend/test/contract.test.ts
autonomous: true
requirements:
  - BACK-01
  - BACK-03
user_setup: []
must_haves:
  truths:
    - JSON and human-readable output are generated from the same normalized backend model.
    - Provider output clearly shows source mode, structured error state, and last refresh metadata.
    - Diagnostics are available on demand without polluting the default contract.
  artifacts:
    - apps/backend/src/formatters/text-formatter.ts
    - apps/backend/src/serializers/snapshot-serializer.ts
    - apps/backend/test/output-parity.test.ts
    - apps/backend/test/snapshot-mapping.test.ts
  key_links:
    - The CLI renders both JSON and text from the same serialized snapshot envelope.
    - Snapshot mapping tests prove that source, errors, and timestamps survive normalization.
---

<objective>
Finish the backend command by wiring real coordinator output, dual-format rendering, and diagnostics-aware reporting.

Purpose: Deliver the public backend behavior that the future GNOME extension and terminal users will both rely on.
Output: Working CLI output modes, serializer, text formatter, and parity tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@.planning/phases/01-backend-contract/01-01-PLAN.md
@.planning/phases/01-backend-contract/01-02-PLAN.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the CLI to the coordinator and finalize JSON behavior</name>
  <files>apps/backend/src/cli.ts, apps/backend/src/serializers/snapshot-serializer.ts</files>
  <read_first>apps/backend/src/cli.ts, apps/backend/src/core/backend-coordinator.ts, packages/shared-contract/src/snapshot.ts, .planning/phases/01-backend-contract/01-01-PLAN.md, .planning/phases/01-backend-contract/01-02-PLAN.md</read_first>
  <action>Replace synthetic output with real coordinator output. Keep JSON as the stable machine contract and ensure `--pretty` changes formatting only. Add serializer logic so `--diagnostics` includes the nested diagnostics block and the default path omits it. Keep all output fields aligned with the shared contract package.</action>
  <verify>sed -n '1,260p' apps/backend/src/cli.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/cli.ts` contains `diagnostics`
    - `apps/backend/src/serializers/snapshot-serializer.ts` controls diagnostic inclusion
    - The CLI no longer relies on synthetic placeholder payloads
  </acceptance_criteria>
  <done>The backend CLI now emits real contract data from the runtime coordinator.</done>
</task>

<task type="auto">
  <name>Task 2: Implement human-readable formatting from the normalized model</name>
  <files>apps/backend/src/formatters/text-formatter.ts, apps/backend/src/cli.ts</files>
  <read_first>apps/backend/src/serializers/snapshot-serializer.ts, packages/shared-contract/src/snapshot.ts, .planning/phases/01-backend-contract/01-CONTEXT.md</read_first>
  <action>Create a text formatter that renders a concise per-provider section with provider name, status, source, `updated_at`, reset window, and structured error summary. When diagnostics are disabled, show only user-relevant status. When diagnostics are enabled, append a compact diagnostics section sourced from the same normalized model without refetching data.</action>
  <verify>sed -n '1,240p' apps/backend/src/formatters/text-formatter.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/formatters/text-formatter.ts` contains `source`
    - `apps/backend/src/formatters/text-formatter.ts` contains `updated_at` handling
    - The formatter reads `diagnostics` from normalized snapshots
  </acceptance_criteria>
  <done>Text output is a formatter over the same contract model used for JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity and mapping tests for errors, source, and refresh metadata</name>
  <files>apps/backend/test/output-parity.test.ts, apps/backend/test/snapshot-mapping.test.ts, apps/backend/test/contract.test.ts</files>
  <read_first>apps/backend/src/formatters/text-formatter.ts, apps/backend/src/serializers/snapshot-serializer.ts, .planning/phases/01-backend-contract/01-VALIDATION.md</read_first>
  <action>Create Vitest coverage that compares JSON and text output against the same synthetic snapshots and verifies that `source`, `updated_at`, and structured errors survive normalization. Extend contract tests as needed so the public envelope remains stable while diagnostics stay opt-in.</action>
  <verify>sed -n '1,240p' apps/backend/test/output-parity.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/output-parity.test.ts` contains `diagnostics`
    - `apps/backend/test/snapshot-mapping.test.ts` contains `updated_at`
    - `apps/backend/test/snapshot-mapping.test.ts` contains `source`
  </acceptance_criteria>
  <done>Dual-output parity and snapshot mapping behavior are protected by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- output-parity snapshot-mapping contract`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --provider codex`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --provider codex --json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- JSON and text output tell the same provider status story, including source, errors, and refresh metadata
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-03-SUMMARY.md`
</output>
