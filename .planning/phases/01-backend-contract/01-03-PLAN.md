---
phase: 01-backend-contract
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift
  - CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift
  - CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift
  - CodexBar/Tests/CodexBarTests/UbuntuBackendFormatterTests.swift
  - CodexBar/Tests/CodexBarTests/UbuntuBackendSnapshotMappingTests.swift
  - CodexBar/TestsLinux/UbuntuBackendContractTests.swift
autonomous: true
requirements:
  - BACK-01
  - BACK-03
user_setup: []
must_haves:
  truths:
    - JSON and human-readable output are generated from the same normalized backend snapshot model.
    - Provider output clearly shows source mode, structured error state, and last refresh metadata.
    - Diagnostics are available on demand without polluting the default text or JSON contract.
  artifacts:
    - CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift
    - CodexBar/Tests/CodexBarTests/UbuntuBackendFormatterTests.swift
    - CodexBar/Tests/CodexBarTests/UbuntuBackendSnapshotMappingTests.swift
  key_links:
    - BackendUsageCommand consumes UbuntuBackendCoordinator and renders both JSON and text from the same envelope.
    - Snapshot mapping tests prove errors, source, and timestamps survive normalization.
---

<objective>
Complete the backend command by wiring real coordinator output, dual-format rendering, and diagnostics-aware reporting.

Purpose: Finish the public backend behavior so Ubuntu surfaces can consume JSON while humans can inspect the same state from the terminal.
Output: A working backend command, shared text formatter, and contract tests for output parity and snapshot mapping.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@.planning/phases/01-backend-contract/01-01-PLAN.md
@.planning/phases/01-backend-contract/01-02-PLAN.md
@CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift
@CodexBar/Sources/CodexBarCLI/CLIUsageCommand.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the backend command to the coordinator and finalize JSON behavior</name>
  <files>CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift, CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift</files>
  <read_first>CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift, CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift, .planning/phases/01-backend-contract/01-01-PLAN.md, .planning/phases/01-backend-contract/01-02-PLAN.md</read_first>
  <action>Replace the placeholder envelope generation with a real `UbuntuBackendCoordinator` call. Keep JSON as the stable machine contract and ensure `--pretty` only changes formatting, not fields. Add a backend-local option flow so `--diagnostics` includes the nested diagnostics block and the default path omits it. Preserve the existing `usage` command behavior and keep backend flags scoped to `backend usage`.</action>
  <verify>sed -n '1,260p' CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift` contains `UbuntuBackendCoordinator`
    - `CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift` contains `diagnostics`
    - `CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift` does not change the legacy `usage` defaults
  </acceptance_criteria>
  <done>The backend command executes the real Linux coordinator and keeps JSON contract behavior stable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement human-readable formatting from the normalized model</name>
  <files>CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift, CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift</files>
  <read_first>CodexBar/Sources/CodexBarCLI/CLIUsageCommand.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift, .planning/phases/01-backend-contract/01-CONTEXT.md</read_first>
  <action>Create `UbuntuBackendTextFormatter` that renders a concise per-provider section showing provider name, status, source, `updated_at`, reset window when present, and structured error summary when failed. When `--diagnostics` is absent, suppress fetch-attempt detail and show only end-user-relevant status. When `--diagnostics` is present, append a compact diagnostics subsection sourced from the same envelope instead of refetching provider data.</action>
  <verify>sed -n '1,240p' CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift` contains `updated_at` or a formatter for `updatedAt`
    - `CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift` contains `source`
    - The formatter reads `diagnostics` from the snapshot model instead of from provider-specific raw types
  </acceptance_criteria>
  <done>Text output is a formatter over the normalized backend model, not a separate semantics path.</done>
</task>

<task type="auto">
  <name>Task 3: Add parity and mapping tests for errors, source, and refresh metadata</name>
  <files>CodexBar/Tests/CodexBarTests/UbuntuBackendFormatterTests.swift, CodexBar/Tests/CodexBarTests/UbuntuBackendSnapshotMappingTests.swift, CodexBar/TestsLinux/UbuntuBackendContractTests.swift</files>
  <read_first>.planning/phases/01-backend-contract/01-VALIDATION.md, CodexBar/Sources/CodexBarCLI/UbuntuBackendTextFormatter.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuProviderSnapshotMapper.swift</read_first>
  <action>Create formatter tests that compare JSON and text output against the same synthetic envelope and mapping tests that verify `source`, `updated_at`, and structured errors survive normalization from provider fetch results. Add one Linux-facing smoke test that runs `backend usage` in text and JSON modes against synthetic data so the manual validation row in `01-VALIDATION.md` has automated support wherever possible.</action>
  <verify>sed -n '1,260p' CodexBar/Tests/CodexBarTests/UbuntuBackendFormatterTests.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Tests/CodexBarTests/UbuntuBackendFormatterTests.swift` contains `diagnostics`
    - `CodexBar/Tests/CodexBarTests/UbuntuBackendSnapshotMappingTests.swift` contains `updatedAt`
    - `CodexBar/Tests/CodexBarTests/UbuntuBackendSnapshotMappingTests.swift` contains `source`
  </acceptance_criteria>
  <done>Dual-output parity and mapping behavior are covered by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd CodexBar && swift test --filter UbuntuBackendFormatterTests`
- [ ] `cd CodexBar && swift test --filter UbuntuBackendSnapshotMappingTests`
- [ ] `cd CodexBar && swift test --filter UbuntuBackendContractTests`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- JSON and text output tell the same provider status story, including source, errors, and refresh metadata
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-03-SUMMARY.md`
</output>
