---
phase: 01-backend-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendRequest.swift
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCache.swift
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuProviderSnapshotMapper.swift
  - CodexBar/TestsLinux/UbuntuBackendContractTests.swift
autonomous: true
requirements:
  - BACK-02
user_setup: []
must_haves:
  truths:
    - Refresh behavior is centralized above provider fetchers and supports all-providers or single-provider execution.
    - Default reads can return cached snapshots within a short TTL, and a forced refresh bypasses that cache.
    - Linux execution reuses `ProviderDescriptorRegistry` and `ProviderFetchContext` instead of duplicating provider logic.
  artifacts:
    - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift
    - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCache.swift
    - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuProviderSnapshotMapper.swift
  key_links:
    - UbuntuBackendCoordinator builds provider fetch contexts from existing descriptor-driven infrastructure.
    - UbuntuBackendCache is consulted before provider execution unless force refresh is requested.
---

<objective>
Implement the Linux-safe refresh coordinator and cache policy above existing provider execution.

Purpose: Make the backend cheap to poll and predictable to debug without burying TTL logic inside provider strategies.
Output: Request/coordinator/cache types, normalized snapshot mapping, and Linux tests for cache-hit versus forced-refresh behavior.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@CodexBar/Sources/CodexBarCore/Providers/ProviderDescriptor.swift
@CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift
@CodexBar/Sources/CodexBarCore/Host/PTY/TTYCommandRunner.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce request and cache primitives for Linux refresh semantics</name>
  <files>CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendRequest.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCache.swift</files>
  <read_first>.planning/phases/01-backend-contract/01-CONTEXT.md, CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift</read_first>
  <action>Create `UbuntuBackendRequest` with exact fields `providers`, `sourceModeOverride`, `forceRefresh`, `includeDiagnostics`, and `ttlSeconds`. Implement `UbuntuBackendCache` keyed by provider identifier plus effective source mode. Use a documented default TTL of `30` seconds when `ttlSeconds` is not supplied. The cache must store the normalized snapshot together with fetch metadata and expose `isExpired` logic; do not add caching to provider strategies or `ProviderDescriptor`.</action>
  <verify>sed -n '1,240p' CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendRequest.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendRequest.swift` contains `ttlSeconds`
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCache.swift` contains `30`
    - `UbuntuBackendCache` exposes an expiration check instead of embedding TTL in provider code
  </acceptance_criteria>
  <done>Refresh policy is represented by explicit backend-layer request and cache types.</done>
</task>

<task type="auto">
  <name>Task 2: Build the provider coordinator on top of descriptor-driven execution</name>
  <files>CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuProviderSnapshotMapper.swift</files>
  <read_first>CodexBar/Sources/CodexBarCore/Providers/ProviderDescriptor.swift, CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift, .planning/phases/01-backend-contract/01-RESEARCH.md</read_first>
  <action>Implement `UbuntuBackendCoordinator.fetchSnapshots(request:)` that resolves one provider or all registered providers via `ProviderDescriptorRegistry`, builds `ProviderFetchContext(runtime: .cli, ...)`, executes each descriptor, and maps the `ProviderFetchOutcome` through `UbuntuProviderSnapshotMapper`. The mapper must populate `source`, `updated_at`, `error`, and optional diagnostics from fetch attempts. On Linux, keep the current platform gate that rejects web-only execution paths instead of silently downgrading them.</action>
  <verify>sed -n '1,260p' CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift` contains `ProviderDescriptorRegistry`
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift` contains `forceRefresh`
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuProviderSnapshotMapper.swift` contains `attempts`
  </acceptance_criteria>
  <done>The backend can execute providers through existing fetch plans while staying Linux-safe and cache-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Linux tests for cache-hit and forced-refresh behavior</name>
  <files>CodexBar/TestsLinux/UbuntuBackendContractTests.swift</files>
  <read_first>CodexBar/TestsLinux/UbuntuBackendContractTests.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendCoordinator.swift, .planning/phases/01-backend-contract/01-VALIDATION.md</read_first>
  <action>Add deterministic tests that execute the coordinator with a stubbed descriptor or synthetic fetch outcome, prove that two reads within the TTL reuse cached data, and prove that `forceRefresh` triggers a new fetch. Verify both the all-providers path and a single-provider path without any live provider dependency.</action>
  <verify>sed -n '1,260p' CodexBar/TestsLinux/UbuntuBackendContractTests.swift</verify>
  <acceptance_criteria>
    - `CodexBar/TestsLinux/UbuntuBackendContractTests.swift` contains `forceRefresh`
    - `CodexBar/TestsLinux/UbuntuBackendContractTests.swift` contains `ttlSeconds`
    - The test file contains separate assertions for one provider and multiple providers
  </acceptance_criteria>
  <done>Cache and refresh semantics are covered by Linux-safe automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd CodexBar && swift test --filter UbuntuBackendContractTests`
- [ ] `cd CodexBar && swift test --filter CodexBarLinuxTests`
- [ ] A forced-refresh run produces a later `generated_at` timestamp than a cache-hit run
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The backend coordinator can refresh one provider or all providers with centralized TTL behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-02-SUMMARY.md`
</output>
