---
phase: 01-backend-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift
  - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendEnvelope.swift
  - CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift
  - CodexBar/Sources/CodexBarCLI/CLIEntry.swift
  - CodexBar/TestsLinux/UbuntuBackendContractTests.swift
autonomous: true
requirements:
  - BACK-01
user_setup: []
must_haves:
  truths:
    - A Linux-facing backend command exists without depending on AppKit or any GNOME-specific code.
    - The backend command can emit a normalized JSON envelope for one provider or all enabled providers.
    - The JSON contract exposes stable top-level metadata and a provider snapshot array instead of provider-specific raw payloads.
  artifacts:
    - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift
    - CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendEnvelope.swift
    - CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift
    - CodexBar/TestsLinux/UbuntuBackendContractTests.swift
  key_links:
    - CLIEntry routes a dedicated backend command to BackendUsageCommand.
    - BackendUsageCommand serializes UbuntuBackendEnvelope using the shared snapshot model from CodexBarCore.
---

<objective>
Define the stable Ubuntu-facing snapshot schema and expose it through a dedicated CLI backend command.

Purpose: Lock the public contract before refresh policy and formatting details spread across the codebase.
Output: Shared contract types in `CodexBarCore`, a dedicated backend CLI entrypoint, and contract-focused Linux tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@CodexBar/Sources/CodexBarCLI/CLIEntry.swift
@CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift
@CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the normalized Ubuntu backend snapshot model</name>
  <files>CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendEnvelope.swift</files>
  <read_first>CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift, .planning/phases/01-backend-contract/01-CONTEXT.md, .planning/phases/01-backend-contract/01-RESEARCH.md</read_first>
  <action>Create shell-agnostic `Codable` and `Sendable` contract types in `CodexBarCore` for the Ubuntu backend surface. Use exact identifiers `UbuntuBackendEnvelope`, `UbuntuProviderSnapshot`, `UbuntuUsagePayload`, `UbuntuRefreshMetadata`, `UbuntuProviderError`, and `UbuntuProviderDiagnostics`. The envelope must include `schema_version`, `generated_at`, and `providers`. The provider snapshot must include `provider`, `source`, `updated_at`, `status`, optional `usage`, optional `reset_window`, optional `error`, and optional `diagnostics`. Keep diagnostics nested and optional; do not flatten fetch attempts into the top level.</action>
  <verify>sed -n '1,240p' CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendSnapshot.swift` contains `public struct UbuntuProviderSnapshot`
    - `CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendEnvelope.swift` contains `schemaVersion`
    - `UbuntuProviderSnapshot` includes `source`, `updatedAt`, and `error`
  </acceptance_criteria>
  <done>The shared contract model exists in `CodexBarCore` and matches the locked dual-output snapshot semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated backend CLI interface for the contract</name>
  <files>CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift, CodexBar/Sources/CodexBarCLI/CLIEntry.swift</files>
  <read_first>CodexBar/Sources/CodexBarCLI/CLIEntry.swift, CodexBar/Sources/CodexBarCLI/CLIOutputPreferences.swift, .planning/ROADMAP.md</read_first>
  <action>Add a new subcommand path `backend usage` instead of overloading the legacy `usage` command. Register it in `CLIEntry.commandDescriptors()` with an abstract string that explicitly says it prints Ubuntu backend snapshots as text or JSON. Implement `BackendUsageCommand.swift` with Commander option decoding for `--provider`, `--format`, `--pretty`, `--refresh`, and `--diagnostics`. The command should build a request object and serialize an empty-but-valid `UbuntuBackendEnvelope` during this plan, deferring real provider execution to later plans. Do not change the behavior of the existing `usage` command.</action>
  <verify>sed -n '1,220p' CodexBar/Sources/CodexBarCLI/CLIEntry.swift</verify>
  <acceptance_criteria>
    - `CodexBar/Sources/CodexBarCLI/CLIEntry.swift` contains `name: "backend"`
    - `CodexBar/Sources/CodexBarCLI/CLIEntry.swift` routes `["backend", "usage"]`
    - `CodexBar/Sources/CodexBarCLI/BackendUsageCommand.swift` contains `--refresh`
  </acceptance_criteria>
  <done>The CLI exposes a stable backend-specific entrypoint without breaking existing commands.</done>
</task>

<task type="auto">
  <name>Task 3: Add Linux contract tests for schema encoding and command wiring</name>
  <files>CodexBar/TestsLinux/UbuntuBackendContractTests.swift</files>
  <read_first>CodexBar/Package.swift, CodexBar/Sources/CodexBarCLI/CLIEntry.swift, CodexBar/Sources/CodexBarCore/UbuntuBackend/UbuntuBackendEnvelope.swift</read_first>
  <action>Create Linux-safe tests that assert `UbuntuBackendEnvelope` encodes the expected top-level keys and that the backend command path resolves through the CLI entrypoint. Keep the tests deterministic and independent of live providers; use fixture snapshots or synthetic instances rather than network or browser-backed flows.</action>
  <verify>sed -n '1,220p' CodexBar/TestsLinux/UbuntuBackendContractTests.swift</verify>
  <acceptance_criteria>
    - `CodexBar/TestsLinux/UbuntuBackendContractTests.swift` contains `UbuntuBackendEnvelope`
    - `CodexBar/TestsLinux/UbuntuBackendContractTests.swift` contains `backend`
    - The test file does not reference AppKit or WebKit
  </acceptance_criteria>
  <done>Linux contract tests exist for the public JSON envelope and CLI surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd CodexBar && swift test --filter UbuntuBackendContractTests`
- [ ] `cd CodexBar && swift test --filter PlatformGatingTests`
- [ ] `codexbar backend usage --format json` emits a JSON object with `schema_version`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A dedicated backend command exists and encodes the normalized contract model
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-01-SUMMARY.md`
</output>
