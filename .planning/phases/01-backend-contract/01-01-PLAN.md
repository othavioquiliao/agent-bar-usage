---
phase: 01-backend-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - packages/shared-contract/package.json
  - packages/shared-contract/src/request.ts
  - packages/shared-contract/src/snapshot.ts
  - packages/shared-contract/src/index.ts
  - apps/backend/package.json
  - apps/backend/tsconfig.json
  - apps/backend/src/cli.ts
  - apps/backend/test/contract.test.ts
autonomous: true
requirements:
  - BACK-01
user_setup: []
must_haves:
  truths:
    - A Linux-facing backend CLI exists in Node.js/TypeScript and does not depend on the nested Swift repo at runtime.
    - The backend can emit a normalized JSON envelope for one provider or all enabled providers.
    - Contract types live in a shared package instead of being buried inside command code.
  artifacts:
    - packages/shared-contract/src/request.ts
    - packages/shared-contract/src/snapshot.ts
    - apps/backend/src/cli.ts
    - apps/backend/test/contract.test.ts
  key_links:
    - The backend CLI imports the shared contract package for request and response shapes.
    - Contract tests validate the same JSON envelope that the future GNOME extension will consume.
---

<objective>
Scaffold the Node.js/TypeScript workspace and lock the shared backend contract.

Purpose: Establish the new implementation base after rejecting Swift reuse, while preserving a stable JSON contract for the future GNOME extension.
Output: Root workspace config, shared-contract package, initial backend CLI entrypoint, and contract tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-contract/01-CONTEXT.md
@.planning/phases/01-backend-contract/01-RESEARCH.md
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
@CodexBar/Sources/CodexBarCLI/CLIUsageCommand.swift
@CodexBar/Sources/CodexBarCore/Providers/ProviderFetchPlan.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the root TypeScript workspace and shared contract package</name>
  <files>package.json, pnpm-workspace.yaml, tsconfig.base.json, packages/shared-contract/package.json, packages/shared-contract/src/request.ts, packages/shared-contract/src/snapshot.ts, packages/shared-contract/src/index.ts</files>
  <read_first>.planning/phases/01-backend-contract/01-CONTEXT.md, .planning/phases/01-backend-contract/01-RESEARCH.md, ubuntu-extension-analysis/gjs-node-v1-architecture.md</read_first>
  <action>Create a root `pnpm` workspace with one shared package named `shared-contract`. Define the canonical request and response schemas there using TypeScript plus `zod`. The shared contract must include exact request fields `providers`, `source_mode_override`, `force_refresh`, `include_diagnostics`, and `ttl_seconds`, and response envelope fields `schema_version`, `generated_at`, and `providers`.</action>
  <verify>sed -n '1,240p' packages/shared-contract/src/snapshot.ts</verify>
  <acceptance_criteria>
    - `packages/shared-contract/src/request.ts` contains `ttl_seconds`
    - `packages/shared-contract/src/snapshot.ts` contains `schema_version`
    - `pnpm-workspace.yaml` includes `apps/*` and `packages/*`
  </acceptance_criteria>
  <done>The Node workspace exists and the contract is centralized in a shared package.</done>
</task>

<task type="auto">
  <name>Task 2: Add the backend CLI entrypoint and command skeleton</name>
  <files>apps/backend/package.json, apps/backend/tsconfig.json, apps/backend/src/cli.ts</files>
  <read_first>ubuntu-extension-analysis/gjs-node-v1-architecture.md, CodexBar/Sources/CodexBarCLI/CLIUsageCommand.swift, .planning/ROADMAP.md</read_first>
  <action>Create the `apps/backend` package with a CLI entrypoint exposed as `agent-bar`. Use `commander` to add a `usage` command with flags `--provider`, `--json`, `--pretty`, `--refresh`, and `--diagnostics`. In this plan, the command may return synthetic but schema-valid data while the runtime coordinator is still pending. Do not import anything from `CodexBar`; use it only as behavior reference.</action>
  <verify>sed -n '1,240p' apps/backend/src/cli.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/cli.ts` contains `usage`
    - `apps/backend/src/cli.ts` contains `--refresh`
    - `apps/backend/src/cli.ts` imports from `shared-contract`
  </acceptance_criteria>
  <done>The new backend CLI exists and speaks the shared contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract tests for JSON envelope and request parsing</name>
  <files>apps/backend/test/contract.test.ts</files>
  <read_first>apps/backend/src/cli.ts, packages/shared-contract/src/request.ts, packages/shared-contract/src/snapshot.ts, .planning/phases/01-backend-contract/01-VALIDATION.md</read_first>
  <action>Create Vitest coverage that validates the request parser and the JSON envelope. The tests must assert that the backend emits `schema_version`, `generated_at`, and a `providers` array, and that invalid request fields are rejected by the shared schema.</action>
  <verify>sed -n '1,240p' apps/backend/test/contract.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/contract.test.ts` contains `schema_version`
    - `apps/backend/test/contract.test.ts` contains `ttl_seconds`
    - `apps/backend/test/contract.test.ts` uses `vitest`
  </acceptance_criteria>
  <done>Phase 1 has deterministic tests for the public backend contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- contract`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --json`
- [ ] The JSON output includes `schema_version`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A Node.js/TypeScript backend CLI exists and emits the normalized contract shape
  </success_criteria>

<output>
After completion, create `.planning/phases/01-backend-contract/01-01-SUMMARY.md`
</output>
