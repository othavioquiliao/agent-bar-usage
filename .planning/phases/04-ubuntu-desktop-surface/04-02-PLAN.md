---
phase: 04-ubuntu-desktop-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - apps/gnome-extension/services/backend-client.js
  - apps/gnome-extension/services/polling-service.js
  - apps/gnome-extension/state/extension-state.js
  - apps/gnome-extension/utils/backend-command.js
  - apps/gnome-extension/utils/time.js
  - apps/gnome-extension/test/backend-client.test.js
  - apps/gnome-extension/test/polling-service.test.js
  - apps/gnome-extension/extension.js
autonomous: true
requirements:
  - UI-01
  - UI-03
user_setup: []
must_haves:
  truths:
    - The extension can invoke the backend CLI asynchronously and parse normalized JSON envelopes.
    - Refresh polling and manual refresh share one controlled state machine instead of overlapping subprocess calls.
    - Backend failures are surfaced as extension state, not as silent failures or unhandled exceptions.
  artifacts:
    - apps/gnome-extension/services/backend-client.js
    - apps/gnome-extension/services/polling-service.js
    - apps/gnome-extension/test/backend-client.test.js
    - apps/gnome-extension/test/polling-service.test.js
  key_links:
    - `backend-client.js` consumes the backend contract already delivered in `apps/backend`.
    - `polling-service.js` mutates state through `state/extension-state.js` rather than building ad-hoc state inline.
---

<objective>
Implement the backend-to-extension bridge and refresh polling model.

Purpose: give the GNOME extension a reliable, non-blocking way to run the backend, parse snapshot envelopes, update state, and avoid overlapping refreshes.
Output: backend command resolution, subprocess-backed client, polling/manual-refresh orchestration, and deterministic tests for pure bridge modules.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md
@.planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md
@.planning/phases/03-first-wave-providers/03-VERIFICATION.md
@.planning/phases/04-ubuntu-desktop-surface/04-01-PLAN.md
@apps/backend/src/cli.ts
@packages/shared-contract/src/snapshot.ts
@ubuntu-extension-analysis/gjs-node-v1-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend command resolution and the GJS subprocess client</name>
  <files>apps/gnome-extension/utils/backend-command.js, apps/gnome-extension/services/backend-client.js, apps/gnome-extension/test/backend-client.test.js</files>
  <read_first>apps/backend/src/cli.ts, packages/shared-contract/src/snapshot.ts, .planning/phases/04-ubuntu-desktop-surface/04-RESEARCH.md, apps/gnome-extension/state/extension-state.js</read_first>
  <action>Create `apps/gnome-extension/utils/backend-command.js` with a deterministic backend argv resolver that tries `agent-bar` on `PATH` first and then a workspace-local dev fallback pointing at `apps/backend/src/cli.ts` via `node --import tsx`. Create `apps/gnome-extension/services/backend-client.js` that uses `Gio.Subprocess` with argv-based execution, calls `usage --json`, supports a `forceRefresh` option that adds `--refresh`, captures stdout/stderr, and parses JSON through the pure helper modules. Add `apps/gnome-extension/test/backend-client.test.js` to validate argv building, JSON parsing, and provider-level backend error propagation using pure helper seams and subprocess result stubs.</action>
  <verify>sed -n '1,280p' apps/gnome-extension/services/backend-client.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/utils/backend-command.js` contains `agent-bar`
    - `apps/gnome-extension/utils/backend-command.js` contains `apps/backend/src/cli.ts`
    - `apps/gnome-extension/services/backend-client.js` contains `Gio.Subprocess`
    - `apps/gnome-extension/services/backend-client.js` contains `--refresh`
  </acceptance_criteria>
  <done>The extension can run the backend CLI through a deterministic bridge layer.</done>
</task>

<task type="auto">
  <name>Task 2: Build polling and refresh orchestration on top of the extension state seam</name>
  <files>apps/gnome-extension/services/polling-service.js, apps/gnome-extension/state/extension-state.js, apps/gnome-extension/utils/time.js, apps/gnome-extension/test/polling-service.test.js</files>
  <read_first>apps/gnome-extension/state/extension-state.js, apps/gnome-extension/services/backend-client.js, .planning/phases/04-ubuntu-desktop-surface/04-VALIDATION.md</read_first>
  <action>Create `apps/gnome-extension/services/polling-service.js` with explicit `start()`, `stop()`, and `refreshNow()` methods, a default 30-second polling interval, an in-flight guard that prevents concurrent backend requests, and a clear way to inject callbacks for state updates. Extend `apps/gnome-extension/state/extension-state.js` so it can store `lastUpdatedText`, `isLoading`, `lastError`, and the latest snapshot envelope. Create `apps/gnome-extension/utils/time.js` for display-safe relative/absolute timestamp formatting that stays free of GNOME Shell imports. Add `apps/gnome-extension/test/polling-service.test.js` covering start/stop behavior, single-flight refresh protection, and successful/error state transitions.</action>
  <verify>sed -n '1,280p' apps/gnome-extension/services/polling-service.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/services/polling-service.js` contains `start()`
    - `apps/gnome-extension/services/polling-service.js` contains `stop()`
    - `apps/gnome-extension/services/polling-service.js` contains `refreshNow()`
    - `apps/gnome-extension/test/polling-service.test.js` contains a single-flight or overlapping-refresh assertion
  </acceptance_criteria>
  <done>The extension has a reusable refresh lifecycle with controlled polling and failure handling.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the bridge into extension lifecycle without building the final menu yet</name>
  <files>apps/gnome-extension/extension.js, apps/gnome-extension/panel/indicator.js, apps/gnome-extension/services/backend-client.js, apps/gnome-extension/services/polling-service.js</files>
  <read_first>apps/gnome-extension/extension.js, apps/gnome-extension/panel/indicator.js, apps/gnome-extension/services/polling-service.js, apps/gnome-extension/services/backend-client.js</read_first>
  <action>Update `apps/gnome-extension/extension.js` so `enable()` creates the backend client and polling service, kicks off an initial refresh, and stores teardown handles on the extension instance. Update the indicator layer just enough to receive state updates for loading, ok, and error summary states without implementing the full provider-detail popup yet. Ensure `disable()` stops polling, disconnects update callbacks, destroys the indicator, and nulls every long-lived reference.</action>
  <verify>sed -n '1,260p' apps/gnome-extension/extension.js</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/extension.js` contains a backend client initialization
    - `apps/gnome-extension/extension.js` contains a polling service initialization
    - `apps/gnome-extension/extension.js` contains a stop or teardown call in `disable()`
    - `apps/gnome-extension/panel/indicator.js` contains update methods for loading or error summary state
  </acceptance_criteria>
  <done>The extension lifecycle is connected to real backend refresh flow and ready for detailed menu rendering.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter gnome-extension test -- backend-client polling-service`
- [ ] `pnpm --filter backend test -- output-parity contract`
- [ ] `apps/gnome-extension/services/backend-client.js` uses `Gio.Subprocess` and the extension lifecycle stops polling cleanly on disable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The GNOME extension can invoke the backend, parse JSON, manage refresh state, and avoid overlapping requests
  </success_criteria>

<output>
After completion, create `.planning/phases/04-ubuntu-desktop-surface/04-02-SUMMARY.md`
</output>
