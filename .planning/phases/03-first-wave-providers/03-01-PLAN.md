---
phase: 03-first-wave-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/providers/copilot/copilot-token-resolver.ts
  - apps/backend/src/providers/copilot/copilot-usage-fetcher.ts
  - apps/backend/src/providers/copilot/copilot-adapter.ts
  - apps/backend/src/core/provider-registry-factory.ts
  - apps/backend/src/cli.ts
  - apps/backend/test/copilot-provider.test.ts
autonomous: true
requirements:
  - COP-01
user_setup: []
must_haves:
  truths:
    - Copilot usage can be fetched through a Linux API/token path and mapped to normalized snapshots.
    - Copilot adapter is integrated via provider registry construction instead of inline mocks.
    - Missing/invalid tokens produce provider-level structured errors, not process-level crashes.
  artifacts:
    - apps/backend/src/providers/copilot/copilot-adapter.ts
    - apps/backend/src/providers/copilot/copilot-usage-fetcher.ts
    - apps/backend/test/copilot-provider.test.ts
  key_links:
    - Copilot adapter consumes runtime context and emits contract-valid snapshots.
    - CLI provider registry now includes real adapter wiring for Copilot.
---

<objective>
Implement the first real provider adapter: Copilot Linux API flow and snapshot mapping.

Purpose: Replace mock Copilot behavior with transport-backed backend logic while preserving the normalized contract consumed by CLI and future GNOME UI.
Output: Copilot token resolution, API fetcher, adapter integration in registry factory, and deterministic Copilot tests.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-first-wave-providers/03-CONTEXT.md
@.planning/phases/03-first-wave-providers/03-RESEARCH.md
@.planning/phases/02-linux-config-secrets/02-VERIFICATION.md
@CodexBar/Sources/CodexBarCore/Providers/Copilot/CopilotProviderDescriptor.swift
@CodexBar/Sources/CodexBarCore/Providers/Copilot/CopilotUsageFetcher.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Copilot token resolution and API fetch mapping</name>
  <files>apps/backend/src/providers/copilot/copilot-token-resolver.ts, apps/backend/src/providers/copilot/copilot-usage-fetcher.ts</files>
  <read_first>.planning/phases/03-first-wave-providers/03-CONTEXT.md, .planning/phases/03-first-wave-providers/03-RESEARCH.md, CodexBar/Sources/CodexBarCore/Providers/Copilot/CopilotUsageFetcher.swift</read_first>
  <action>Implement a Copilot token resolver (environment-first) and API fetcher that calls the supported Copilot endpoint, handles auth failures cleanly, and maps provider payload into normalized usage/reset structures used by the backend contract.</action>
  <verify>sed -n '1,260p' apps/backend/src/providers/copilot/copilot-usage-fetcher.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/providers/copilot/copilot-usage-fetcher.ts` contains the Copilot API endpoint
    - Auth failures map to structured provider-level errors
    - Fetch mapping yields contract-compatible usage fields
  </acceptance_criteria>
  <done>Copilot API data can be fetched and translated into normalized backend snapshot inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Copilot adapter and integrate provider registry factory</name>
  <files>apps/backend/src/providers/copilot/copilot-adapter.ts, apps/backend/src/core/provider-registry-factory.ts, apps/backend/src/cli.ts</files>
  <read_first>apps/backend/src/core/provider-adapter.ts, apps/backend/src/core/provider-context-builder.ts, apps/backend/src/cli.ts, .planning/ROADMAP.md</read_first>
  <action>Create a Copilot adapter implementing `ProviderAdapter`, then move provider construction into a registry factory module and update `cli.ts` to consume that factory. Preserve diagnostics and normalized output behavior from previous phases.</action>
  <verify>sed -n '1,260p' apps/backend/src/providers/copilot/copilot-adapter.ts</verify>
  <acceptance_criteria>
    - `apps/backend/src/providers/copilot/copilot-adapter.ts` implements adapter availability and fetch
    - `apps/backend/src/core/provider-registry-factory.ts` exists and registers Copilot
    - `apps/backend/src/cli.ts` no longer hardcodes all provider mocks inline
  </acceptance_criteria>
  <done>Copilot is a real runtime adapter wired through centralized provider registration.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic tests for Copilot provider behavior</name>
  <files>apps/backend/test/copilot-provider.test.ts</files>
  <read_first>apps/backend/src/providers/copilot/copilot-adapter.ts, apps/backend/src/providers/copilot/copilot-usage-fetcher.ts, .planning/phases/03-first-wave-providers/03-VALIDATION.md</read_first>
  <action>Add Vitest coverage for token-missing, auth-error, and success mapping cases. Tests must avoid real network calls by mocking fetch boundaries and must verify the adapter emits normalized snapshot semantics.</action>
  <verify>sed -n '1,260p' apps/backend/test/copilot-provider.test.ts</verify>
  <acceptance_criteria>
    - `apps/backend/test/copilot-provider.test.ts` contains `copilot`
    - Tests cover missing token behavior
    - Tests cover successful snapshot mapping
  </acceptance_criteria>
  <done>Copilot runtime behavior is protected by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test -- copilot-provider`
- [ ] `pnpm --filter backend exec tsx src/cli.ts usage --provider copilot --json`
- [ ] Copilot output includes `provider: "copilot"` and contract-valid `source`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Copilot provider is implemented through Linux API/token path and integrated into backend runtime
  </success_criteria>

<output>
After completion, create `.planning/phases/03-first-wave-providers/03-01-SUMMARY.md`
</output>
