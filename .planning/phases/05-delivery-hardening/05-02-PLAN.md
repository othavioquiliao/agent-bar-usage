---
phase: 05-delivery-hardening
plan: 02
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-03"
files_modified:
  - README.md
  - docs/ubuntu-install.md
  - docs/ubuntu-troubleshooting.md
  - docs/ubuntu-debugging.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
user_setup: []
must_haves:
  truths:
    - A user can follow one supported Ubuntu installation path end-to-end per D-03, D-04, and D-05.
    - Troubleshooting guidance maps concrete failures to exact commands rather than vague narrative.
    - Backend-only and GNOME-only debug loops are documented separately so Phase 5 satisfies D-09 and D-10.
  artifacts:
    - docs/ubuntu-install.md
    - docs/ubuntu-troubleshooting.md
    - docs/ubuntu-debugging.md
    - README.md
  key_links:
    - `ubuntu-install.md` documents the actual installer and verification scripts from `05-03`, not a hypothetical future flow.
    - `ubuntu-troubleshooting.md` and `ubuntu-debugging.md` point back to backend diagnostics and service commands rather than inventing separate doc-only procedures.
---

<objective>
Write Ubuntu installation, setup, and troubleshooting docs for the Node backend and GNOME extension.

Purpose: make the supported Phase 5 delivery path discoverable and repeatable for users and contributors by documenting the real install script, verification flow, troubleshooting matrix, and independent backend/UI debug loops.
Output: installation guide, troubleshooting guide, debugging guide, and a root quickstart/index that links the supported commands and docs.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delivery-hardening/05-CONTEXT.md
@.planning/phases/05-delivery-hardening/05-RESEARCH.md
@.planning/phases/05-delivery-hardening/05-VALIDATION.md
@.planning/phases/05-delivery-hardening/05-01-PLAN.md
@.planning/phases/05-delivery-hardening/05-03-PLAN.md
@apps/backend/src/cli.ts
@apps/backend/src/commands/config-command.ts
@apps/backend/src/commands/diagnostics-command.ts
@apps/gnome-extension/metadata.json
@scripts/install-ubuntu.sh
@scripts/verify-ubuntu-install.sh
@packaging/systemd/user/agent-bar.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the supported Ubuntu installation and verification flow</name>
  <files>docs/ubuntu-install.md, README.md</files>
  <read_first>scripts/install-ubuntu.sh, scripts/verify-ubuntu-install.sh, packaging/systemd/user/agent-bar.service, apps/gnome-extension/metadata.json, .planning/phases/05-delivery-hardening/05-RESEARCH.md</read_first>
  <action>Create `docs/ubuntu-install.md` with the exact sections `## Prerequisites`, `## Install`, `## Enable Service`, `## Enable Extension`, and `## Verify`. The documented commands must include `pnpm install`, `pnpm run install:ubuntu`, `pnpm run verify:ubuntu`, and `systemctl --user status agent-bar.service --no-pager`. Document the extension install target as `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev`, and include the manual fallback path when `gnome-extensions` is unavailable. Create or update `README.md` with a short `## Ubuntu Quickstart` section that links directly to `docs/ubuntu-install.md` and names the supported install command.</action>
  <verify>sed -n '1,260p' docs/ubuntu-install.md</verify>
  <acceptance_criteria>
    - `docs/ubuntu-install.md` contains `## Prerequisites`
    - `docs/ubuntu-install.md` contains `pnpm run install:ubuntu`
    - `docs/ubuntu-install.md` contains `systemctl --user status agent-bar.service --no-pager`
    - `README.md` contains `## Ubuntu Quickstart`
  </acceptance_criteria>
  <done>The repository documents one supported Ubuntu install path and links to it from the root entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Write the troubleshooting matrix around diagnostics, service logs, and extension failures</name>
  <files>docs/ubuntu-troubleshooting.md</files>
  <read_first>apps/backend/src/commands/diagnostics-command.ts, apps/backend/src/commands/config-command.ts, scripts/verify-ubuntu-install.sh, .planning/phases/05-delivery-hardening/05-01-PLAN.md, .planning/phases/05-delivery-hardening/05-03-PLAN.md</read_first>
  <action>Create `docs/ubuntu-troubleshooting.md` with sections `## Failure Matrix`, `## Backend Diagnostics`, `## Service Logs`, and `## Extension Failures`. The failure matrix must explicitly cover invalid config, missing `secret-tool`, missing `codex` or `claude` CLI, unhealthy `agent-bar.service`, and missing/disabled GNOME extension state. For each row, include the exact command to run next: `agent-bar doctor --json`, `agent-bar config validate`, `agent-bar service status --json`, `journalctl --user-unit agent-bar.service -f`, and `gnome-extensions enable agent-bar-ubuntu@othavio.dev` when the CLI exists. Keep the doc aligned with D-01 and D-02 by mapping each failure to a concrete explanation and next action.</action>
  <verify>sed -n '1,280p' docs/ubuntu-troubleshooting.md</verify>
  <acceptance_criteria>
    - `docs/ubuntu-troubleshooting.md` contains `## Failure Matrix`
    - `docs/ubuntu-troubleshooting.md` contains `agent-bar doctor --json`
    - `docs/ubuntu-troubleshooting.md` contains `journalctl --user-unit agent-bar.service -f`
    - `docs/ubuntu-troubleshooting.md` contains `gnome-extensions enable agent-bar-ubuntu@othavio.dev`
  </acceptance_criteria>
  <done>Users have a concrete troubleshooting matrix for the supported backend, service, and GNOME failure modes.</done>
</task>

<task type="auto">
  <name>Task 3: Publish separate backend-only and GNOME-only debug workflows</name>
  <files>docs/ubuntu-debugging.md, README.md</files>
  <read_first>docs/ubuntu-install.md, docs/ubuntu-troubleshooting.md, apps/backend/src/cli.ts, apps/gnome-extension/metadata.json, .planning/phases/05-delivery-hardening/05-CONTEXT.md</read_first>
  <action>Create `docs/ubuntu-debugging.md` with the exact sections `## Backend-only smoke`, `## GNOME-only smoke`, and `## Full-stack smoke`. The backend-only section must include `agent-bar usage --json`, `agent-bar doctor --json`, and `agent-bar service status --json`. The GNOME-only section must include `gnome-extensions disable agent-bar-ubuntu@othavio.dev`, `gnome-extensions enable agent-bar-ubuntu@othavio.dev`, and the manual Looking Glass path `Alt+F2`, then `lg`, for shell diagnostics. Update `README.md` so it links to `docs/ubuntu-debugging.md` and names backend-only versus GNOME-only iteration explicitly per D-09 and D-10.</action>
  <verify>sed -n '1,260p' docs/ubuntu-debugging.md</verify>
  <acceptance_criteria>
    - `docs/ubuntu-debugging.md` contains `## Backend-only smoke`
    - `docs/ubuntu-debugging.md` contains `agent-bar usage --json`
    - `docs/ubuntu-debugging.md` contains `gnome-extensions disable agent-bar-ubuntu@othavio.dev`
    - `README.md` contains `docs/ubuntu-debugging.md`
  </acceptance_criteria>
  <done>The repo documents distinct backend, GNOME, and full-stack debug loops instead of requiring the whole product to be running for every check.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f docs/ubuntu-install.md && test -f docs/ubuntu-troubleshooting.md && test -f docs/ubuntu-debugging.md`
- [ ] `docs/ubuntu-install.md` contains `pnpm run install:ubuntu` and `pnpm run verify:ubuntu`
- [ ] `docs/ubuntu-troubleshooting.md` contains `agent-bar doctor --json` and `journalctl --user-unit agent-bar.service -f`
- [ ] `docs/ubuntu-debugging.md` contains both backend-only and GNOME-only smoke sections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- A user can install, troubleshoot, and debug the Ubuntu stack from documented, copy-pasteable commands
  </success_criteria>

<output>
After completion, create `.planning/phases/05-delivery-hardening/05-02-SUMMARY.md`
</output>
