---
phase: 05-delivery-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-contract/src/diagnostics.ts
  - packages/shared-contract/src/index.ts
  - apps/backend/src/commands/diagnostics-command.ts
  - apps/backend/src/core/prerequisite-checks.ts
  - apps/backend/src/cli.ts
  - apps/backend/test/diagnostics-command.test.ts
  - apps/backend/test/prerequisite-checks.test.ts
  - apps/gnome-extension/utils/backend-command.js
  - apps/gnome-extension/services/backend-client.js
  - apps/gnome-extension/utils/view-model.js
  - apps/gnome-extension/panel/menu-builder.js
  - apps/gnome-extension/test/backend-client.test.js
  - apps/gnome-extension/test/view-model.test.js
autonomous: true
requirements:
  - OPS-01
user_setup: []
must_haves:
  truths:
    - The backend exposes a first-class diagnostics command that explains missing prerequisites and configuration/runtime failures instead of only returning raw stderr.
    - The GNOME extension renders actionable diagnostics and prerequisite detail from the same backend-owned story per D-01 and D-02.
    - Diagnostics stay contract-driven and inspectable from both CLI and desktop paths instead of being duplicated in GJS.
  artifacts:
    - packages/shared-contract/src/diagnostics.ts
    - apps/backend/src/commands/diagnostics-command.ts
    - apps/backend/src/core/prerequisite-checks.ts
    - apps/gnome-extension/panel/menu-builder.js
  key_links:
    - `diagnostics-command.ts` reuses backend config, PATH, and secret-store boundaries instead of introducing UI-only checks.
    - `menu-builder.js` and `view-model.js` consume backend diagnostics data rather than inventing a second prerequisite model in the extension.
---

<objective>
Add operational diagnostics and failure-reporting surfaces.

Purpose: implement D-01 and D-02 by giving Ubuntu users one backend-owned diagnostics surface plus a GNOME details/failure affordance that explains missing CLIs, config issues, secret-store problems, and provider/runtime failures.
Output: shared diagnostics schema, `agent-bar doctor` CLI output, prerequisite inspection helpers, and GNOME diagnostics rendering that consumes backend-provided details.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delivery-hardening/05-CONTEXT.md
@.planning/phases/05-delivery-hardening/05-RESEARCH.md
@.planning/phases/05-delivery-hardening/05-VALIDATION.md
@.planning/phases/02-linux-config-secrets/02-VERIFICATION.md
@.planning/phases/04-ubuntu-desktop-surface/04-02-SUMMARY.md
@.planning/phases/04-ubuntu-desktop-surface/04-03-SUMMARY.md
@packages/shared-contract/src/request.ts
@packages/shared-contract/src/snapshot.ts
@apps/backend/src/cli.ts
@apps/backend/src/commands/config-command.ts
@apps/backend/src/config/config-path.ts
@apps/backend/src/utils/subprocess.ts
@apps/backend/src/secrets/secret-tool-store.ts
@apps/gnome-extension/utils/backend-command.js
@apps/gnome-extension/services/backend-client.js
@apps/gnome-extension/panel/menu-builder.js
@apps/gnome-extension/utils/view-model.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the diagnostics contract and backend doctor command</name>
  <files>packages/shared-contract/src/diagnostics.ts, packages/shared-contract/src/index.ts, apps/backend/src/commands/diagnostics-command.ts, apps/backend/src/cli.ts</files>
  <read_first>packages/shared-contract/src/request.ts, packages/shared-contract/src/snapshot.ts, apps/backend/src/cli.ts, apps/backend/src/commands/config-command.ts, .planning/phases/05-delivery-hardening/05-CONTEXT.md, .planning/phases/05-delivery-hardening/05-RESEARCH.md</read_first>
  <action>Create `packages/shared-contract/src/diagnostics.ts` with strict zod schemas for a diagnostics report that includes `generated_at`, `runtime_mode`, and a `checks` array. Each check must contain `id`, `label`, `status`, `message`, `suggested_command`, and optional `details`, with `status` limited to `ok`, `warn`, or `error`. Export the new diagnostics schema and types from `packages/shared-contract/src/index.ts`. Create `apps/backend/src/commands/diagnostics-command.ts` and register it in `apps/backend/src/cli.ts` as `agent-bar doctor` with `--json` and `--pretty` options. The command must emit the shared diagnostics report in JSON mode and a readable text table in default mode. Per D-01 and D-02, the checks must be named for `config`, `secret-tool`, `codex-cli`, `claude-cli`, `copilot-token`, and `service-runtime` so the rest of the phase can depend on stable identifiers.</action>
  <verify>sed -n '1,260p' apps/backend/src/commands/diagnostics-command.ts</verify>
  <acceptance_criteria>
    - `packages/shared-contract/src/diagnostics.ts` contains `diagnosticsReportSchema`
    - `packages/shared-contract/src/diagnostics.ts` contains `suggested_command`
    - `apps/backend/src/commands/diagnostics-command.ts` contains `doctor`
    - `apps/backend/src/cli.ts` contains `.command("doctor")`
  </acceptance_criteria>
  <done>The backend exposes a stable diagnostics report contract and a dedicated CLI command for it.</done>
</task>

<task type="auto">
  <name>Task 2: Implement prerequisite inspection and backend diagnostics coverage</name>
  <files>apps/backend/src/core/prerequisite-checks.ts, apps/backend/src/commands/diagnostics-command.ts, apps/backend/test/diagnostics-command.test.ts, apps/backend/test/prerequisite-checks.test.ts</files>
  <read_first>apps/backend/src/config/config-path.ts, apps/backend/src/commands/config-command.ts, apps/backend/src/utils/subprocess.ts, apps/backend/src/secrets/secret-tool-store.ts, apps/backend/src/commands/diagnostics-command.ts, .planning/phases/02-linux-config-secrets/02-VERIFICATION.md</read_first>
  <action>Create `apps/backend/src/core/prerequisite-checks.ts` with pure helpers that inspect the resolved config path, validate whether `secret-tool`, `codex`, and `claude` exist on `PATH`, detect whether a Copilot token source is configured, and report the current service/runtime mode. Use exact suggested commands in the report: `agent-bar config validate`, `which secret-tool`, `codex --version`, `claude --version`, and `agent-bar service status --json`. Update `diagnostics-command.ts` to call those helpers and map failures into actionable `message` values rather than raw stderr. Add `apps/backend/test/prerequisite-checks.test.ts` for missing command, configured command, and token-present/token-missing branches. Add `apps/backend/test/diagnostics-command.test.ts` for JSON output shape, text output labels, and at least one missing-prerequisite report line.</action>
  <verify>pnpm --filter backend test</verify>
  <acceptance_criteria>
    - `apps/backend/src/core/prerequisite-checks.ts` contains `which secret-tool`
    - `apps/backend/src/core/prerequisite-checks.ts` contains `agent-bar service status --json`
    - `apps/backend/test/diagnostics-command.test.ts` contains `doctor`
    - `apps/backend/test/prerequisite-checks.test.ts` contains a missing command assertion
  </acceptance_criteria>
  <done>The backend can explain prerequisite and runtime readiness through deterministic diagnostics output and automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Surface actionable diagnostics in the GNOME details view</name>
  <files>apps/gnome-extension/utils/backend-command.js, apps/gnome-extension/services/backend-client.js, apps/gnome-extension/utils/view-model.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/test/backend-client.test.js, apps/gnome-extension/test/view-model.test.js</files>
  <read_first>apps/gnome-extension/utils/backend-command.js, apps/gnome-extension/services/backend-client.js, apps/gnome-extension/utils/view-model.js, apps/gnome-extension/panel/menu-builder.js, packages/shared-contract/src/diagnostics.ts, .planning/phases/05-delivery-hardening/05-RESEARCH.md</read_first>
  <action>Update `apps/gnome-extension/utils/backend-command.js` so installed and workspace-dev usage requests append `--diagnostics` by default. Update `apps/gnome-extension/services/backend-client.js` to preserve diagnostics-rich snapshot payloads and backend failure context without dropping `stderr`, `exitCode`, or `mode`. Extend `apps/gnome-extension/utils/view-model.js` so provider rows and footer details can derive a short diagnostics summary plus one `Suggested command:` line from provider attempts or backend doctor output. Update `apps/gnome-extension/panel/menu-builder.js` to render a `Diagnostics` block in the details section whenever a provider error, missing prerequisite, or backend failure exists. Add/extend tests so `apps/gnome-extension/test/backend-client.test.js` verifies `--diagnostics` is present in the argv, and `apps/gnome-extension/test/view-model.test.js` verifies missing-prerequisite text and suggested command mapping.</action>
  <verify>pnpm --filter gnome-extension test</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/utils/backend-command.js` contains `--diagnostics`
    - `apps/gnome-extension/panel/menu-builder.js` contains `Diagnostics`
    - `apps/gnome-extension/panel/menu-builder.js` contains `Suggested command:`
    - `apps/gnome-extension/test/backend-client.test.js` contains `--diagnostics`
  </acceptance_criteria>
  <done>The GNOME extension exposes explicit, actionable diagnostics instead of only a generic backend error footer.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test`
- [ ] `pnpm --filter gnome-extension test`
- [ ] `pnpm --filter backend exec node --import tsx src/cli.ts doctor --json`
- [ ] `apps/gnome-extension/panel/menu-builder.js` contains a `Diagnostics` section and `apps/gnome-extension/utils/backend-command.js` appends `--diagnostics`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users can inspect backend diagnostics from CLI and see the same failure/prerequisite story reflected in the GNOME details surface
  </success_criteria>

<output>
After completion, create `.planning/phases/05-delivery-hardening/05-01-SUMMARY.md`
</output>
