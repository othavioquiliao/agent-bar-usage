---
phase: 05-delivery-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - package.json
  - apps/backend/package.json
  - apps/backend/tsconfig.build.json
  - apps/backend/src/service/socket-path.ts
  - apps/backend/src/service/service-server.ts
  - apps/backend/src/service/service-client.ts
  - apps/backend/src/commands/service-command.ts
  - apps/backend/src/cli.ts
  - apps/backend/test/service-client.test.ts
  - apps/backend/test/service-command.test.ts
  - apps/gnome-extension/utils/backend-command.js
  - apps/gnome-extension/services/backend-client.js
  - apps/gnome-extension/test/backend-client.test.js
  - packaging/systemd/user/agent-bar.service
  - scripts/install-ubuntu.sh
  - scripts/verify-ubuntu-install.sh
autonomous: true
requirements:
  - OPS-02
  - OPS-03
user_setup: []
must_haves:
  truths:
    - The Ubuntu product has a real local backend service and user-scoped startup path per D-06 and D-07 instead of relying on on-demand subprocess execution only.
    - The service preserves the existing normalized snapshot contract per D-08 and remains callable through explicit service client commands.
    - Packaging, install, and verification scripts create one supported runtime path that can be checked without guessing service or extension state.
  artifacts:
    - apps/backend/src/service/service-server.ts
    - apps/backend/src/commands/service-command.ts
    - packaging/systemd/user/agent-bar.service
    - scripts/install-ubuntu.sh
    - scripts/verify-ubuntu-install.sh
  key_links:
    - `service-server.ts` and `service-client.ts` reuse the existing backend coordinator and shared contract rather than inventing a new service-only payload.
    - `backend-command.js` uses the installed service client path while preserving the workspace-dev fallback for local development.
---

<objective>
Harden packaging, startup behavior, and end-to-end verification.

Purpose: make the Ubuntu v1 runtime installable and supportable by introducing a real local backend service, packaging the installed backend path, wiring the extension to that runtime, and creating deterministic install/verification scripts.
Output: backend service entrypoints, service-aware client commands, build/install scripts, a user systemd unit, and a verification harness for backend plus GNOME startup behavior.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-delivery-hardening/05-CONTEXT.md
@.planning/phases/05-delivery-hardening/05-RESEARCH.md
@.planning/phases/05-delivery-hardening/05-VALIDATION.md
@.planning/phases/05-delivery-hardening/05-01-PLAN.md
@packages/shared-contract/src/request.ts
@packages/shared-contract/src/snapshot.ts
@apps/backend/package.json
@apps/backend/src/cli.ts
@apps/backend/src/core/backend-coordinator.ts
@apps/backend/src/cache/snapshot-cache.ts
@apps/backend/src/utils/subprocess.ts
@apps/gnome-extension/utils/backend-command.js
@apps/gnome-extension/services/backend-client.js
@apps/gnome-extension/metadata.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the backend service runtime and service-aware CLI commands</name>
  <files>apps/backend/src/service/socket-path.ts, apps/backend/src/service/service-server.ts, apps/backend/src/service/service-client.ts, apps/backend/src/commands/service-command.ts, apps/backend/src/cli.ts, apps/backend/test/service-client.test.ts, apps/backend/test/service-command.test.ts</files>
  <read_first>apps/backend/src/cli.ts, apps/backend/src/core/backend-coordinator.ts, apps/backend/src/cache/snapshot-cache.ts, packages/shared-contract/src/request.ts, packages/shared-contract/src/snapshot.ts, .planning/phases/05-delivery-hardening/05-RESEARCH.md</read_first>
  <action>Create `apps/backend/src/service/socket-path.ts` that resolves the Unix socket path `${XDG_RUNTIME_DIR}/agent-bar/service.sock`. Create `apps/backend/src/service/service-server.ts` that starts a long-lived backend process serving JSON requests for `snapshot` and `refresh` and returns the existing snapshot envelope shape from `packages/shared-contract/src/snapshot.ts`. Create `apps/backend/src/service/service-client.ts` with helpers for socket request/response handling. Create `apps/backend/src/commands/service-command.ts` and register it from `apps/backend/src/cli.ts` with the exact commands `agent-bar service run`, `agent-bar service status --json`, `agent-bar service snapshot --json`, and `agent-bar service refresh --json`. `service status --json` must report `mode`, `socket_path`, `running`, and `last_error` fields so D-09 can point to a stable backend-only debug command. Add backend tests for command routing, socket-path resolution, and JSON request/response handling.</action>
  <verify>pnpm --filter backend test</verify>
  <acceptance_criteria>
    - `apps/backend/src/commands/service-command.ts` contains `service run`
    - `apps/backend/src/commands/service-command.ts` contains `service status`
    - `apps/backend/src/service/socket-path.ts` contains `service.sock`
    - `apps/backend/test/service-command.test.ts` contains `service status --json`
  </acceptance_criteria>
  <done>The backend has a real service runtime and explicit service client commands that preserve the existing contract.</done>
</task>

<task type="auto">
  <name>Task 2: Package the installed backend path and user service installation flow</name>
  <files>package.json, apps/backend/package.json, apps/backend/tsconfig.build.json, packaging/systemd/user/agent-bar.service, scripts/install-ubuntu.sh</files>
  <read_first>package.json, apps/backend/package.json, apps/gnome-extension/metadata.json, apps/gnome-extension/utils/backend-command.js, .planning/phases/05-delivery-hardening/05-CONTEXT.md, .planning/phases/05-delivery-hardening/05-RESEARCH.md</read_first>
  <action>Create `apps/backend/tsconfig.build.json` with emitted JS output under `dist/`. Update `apps/backend/package.json` with a `build` script `tsc -p tsconfig.build.json`. Update the root `package.json` with `install:ubuntu` and `verify:ubuntu` scripts pointing at `./scripts/install-ubuntu.sh` and `./scripts/verify-ubuntu-install.sh`. Create `packaging/systemd/user/agent-bar.service` with `ExecStart=%h/.local/bin/agent-bar service run`, `Restart=on-failure`, `WorkingDirectory=%h/.local/share/agent-bar-ubuntu`, and no shell-wrapped command strings. Create `scripts/install-ubuntu.sh` that runs `pnpm install`, builds the backend, installs or updates the backend runtime under `~/.local/share/agent-bar-ubuntu/backend`, installs a launcher at `~/.local/bin/agent-bar`, copies the extension to `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev`, installs the service unit under `~/.config/systemd/user/agent-bar.service`, runs `systemctl --user daemon-reload && systemctl --user enable --now agent-bar.service`, and attempts `gnome-extensions enable agent-bar-ubuntu@othavio.dev` when the CLI exists.</action>
  <verify>sed -n '1,260p' scripts/install-ubuntu.sh</verify>
  <acceptance_criteria>
    - `apps/backend/package.json` contains `"build": "tsc -p tsconfig.build.json"`
    - `package.json` contains `install:ubuntu`
    - `packaging/systemd/user/agent-bar.service` contains `ExecStart=%h/.local/bin/agent-bar service run`
    - `scripts/install-ubuntu.sh` contains `systemctl --user enable --now agent-bar.service`
  </acceptance_criteria>
  <done>The repo has a supported installed runtime path and a user-scoped installer that sets up both backend service and extension assets.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the extension to the service path and add end-to-end verification</name>
  <files>apps/gnome-extension/utils/backend-command.js, apps/gnome-extension/services/backend-client.js, apps/gnome-extension/test/backend-client.test.js, scripts/verify-ubuntu-install.sh, packaging/systemd/user/agent-bar.service</files>
  <read_first>apps/gnome-extension/utils/backend-command.js, apps/gnome-extension/services/backend-client.js, scripts/install-ubuntu.sh, packaging/systemd/user/agent-bar.service, .planning/phases/05-delivery-hardening/05-VALIDATION.md</read_first>
  <action>Update `apps/gnome-extension/utils/backend-command.js` so installed-mode invocations use `agent-bar service snapshot --json` and `agent-bar service refresh --json` while workspace-dev fallback keeps `node --import tsx ... usage --json --diagnostics`. Update `apps/gnome-extension/services/backend-client.js` and tests so force refresh maps to the installed `service refresh` command and failure reporting still preserves `stderr`, `exitCode`, `cwd`, and `mode`. Create `scripts/verify-ubuntu-install.sh` that runs `agent-bar doctor --json`, `agent-bar service status --json`, `systemctl --user status agent-bar.service --no-pager`, `journalctl --user-unit agent-bar.service --since '5 minutes ago' --no-pager`, and one successful `agent-bar service snapshot --json` smoke call. Keep the service unit hardened for startup with `Restart=on-failure` and any required environment/path wiring needed by the installed backend.</action>
  <verify>sed -n '1,260p' scripts/verify-ubuntu-install.sh</verify>
  <acceptance_criteria>
    - `apps/gnome-extension/utils/backend-command.js` contains `service snapshot --json`
    - `apps/gnome-extension/utils/backend-command.js` contains `service refresh --json`
    - `scripts/verify-ubuntu-install.sh` contains `agent-bar doctor --json`
    - `scripts/verify-ubuntu-install.sh` contains `journalctl --user-unit agent-bar.service --since '5 minutes ago' --no-pager`
  </acceptance_criteria>
  <done>The installed extension talks to the supported service runtime and the repo contains a concrete end-to-end verification harness.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter backend test`
- [ ] `pnpm --filter gnome-extension test`
- [ ] `test -f packaging/systemd/user/agent-bar.service && test -f scripts/install-ubuntu.sh && test -f scripts/verify-ubuntu-install.sh`
- [ ] `apps/gnome-extension/utils/backend-command.js` uses `service snapshot --json` in installed mode and `scripts/verify-ubuntu-install.sh` runs `agent-bar service status --json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Ubuntu product has a supported installed runtime, user service startup path, and verification flow that keeps backend and UI contract boundaries intact
  </success_criteria>

<output>
After completion, create `.planning/phases/05-delivery-hardening/05-03-SUMMARY.md`
</output>
