---
phase: 07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gnome-extension/utils/view-model.js
  - apps/gnome-extension/test/view-model.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Indicator copy is aggregate-only and uses the short labels from the UI contract instead of generic provider counts."
    - "Each provider row view model exposes icon, progress, primary metric, and one secondary line while pushing commands and verbose diagnostics into details-only fields."
    - "Provider order, refresh state, and error visibility stay driven by normalized snapshot data rather than widget-local logic."
  artifacts:
    - path: "apps/gnome-extension/utils/view-model.js"
      provides: "Compact summary and provider-row contracts for the redesigned UI"
      exports: ["buildProviderRowViewModel", "buildSnapshotViewModel", "buildIndicatorSummaryViewModel"]
    - path: "apps/gnome-extension/test/view-model.test.js"
      provides: "Regression coverage for indicator labels, progress fields, and concise secondary metadata"
      contains: "healthyCount"
  key_links:
    - from: "apps/gnome-extension/utils/view-model.js"
      to: "apps/gnome-extension/test/view-model.test.js"
      via: "explicit assertions for indicator and provider-row contracts"
      pattern: "healthyCount|issueCount|progressPercent|detailsSuggestedCommandText"
---

<objective>
Redefine the GNOME extension view-model contract so the indicator and menu can render a compact, icon-led, progress-bar-driven UI without leaking verbose diagnostics into the primary scan path.

Purpose: Phase 7 rendering should be a presentation swap on top of normalized data, not a rewrite of backend or polling behavior.
Output: A tested view-model contract for indicator summary, summary row, provider rows, and details/footer content.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-CONTEXT.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-RESEARCH.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
@[apps/gnome-extension/utils/view-model.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/utils/view-model.js)
@[apps/gnome-extension/test/view-model.test.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/test/view-model.test.js)
@[apps/gnome-extension/utils/time.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/utils/time.js)

<interfaces>
Current exported seam:

```js
export function buildProviderRowViewModel(providerSnapshot, { now = new Date() } = {}) { ... }
export function buildSnapshotViewModel(state = {}, { now = new Date() } = {}) { ... }
export function buildIndicatorSummaryViewModel(state = {}, { now = new Date() } = {}) { ... }
```

Target provider row fields for Phase 7 rendering:

```js
{
  providerId,
  title,
  status,
  statusText,
  statusIconName,
  iconKey,
  accentColor,
  quotaText,
  progressPercent,
  progressVisible,
  secondaryText,
  detailsSuggestedCommandText,
  detailsSourceText,
  issueSummaryText,
  updatedAtText,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing view-model tests for the compact indicator and row contract</name>
  <read_first>
    apps/gnome-extension/test/view-model.test.js
    apps/gnome-extension/utils/view-model.js
    apps/gnome-extension/utils/time.js
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-CONTEXT.md
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
  </read_first>
  <files>apps/gnome-extension/test/view-model.test.js</files>
  <behavior>
    - Test 1: healthy indicator state returns `emblem-ok-symbolic` and label text `{healthyCount}/{providerCount} ok`.
    - Test 2: issue indicator state returns `dialog-warning-symbolic` and label text `{issueCount} issue` or `{issueCount} issues`.
    - Test 3: backend failure indicator returns `dialog-error-symbolic` and `Service`.
    - Test 4: provider rows expose `quotaText`, `progressPercent`, `progressVisible`, and one `secondaryText`, while row-level commands move to `detailsSuggestedCommandText`.
    - Test 5: missing prerequisite and auth failures normalize to compact secondary labels such as `Missing secret-tool` or `Auth needed`.
  </behavior>
  <acceptance_criteria>
    - `apps/gnome-extension/test/view-model.test.js` contains assertions for `healthyCount`, `issueCount`, `progressPercent`, `progressVisible`, `secondaryText`, and `detailsSuggestedCommandText`.
    - `apps/gnome-extension/test/view-model.test.js` contains the exact indicator copy strings `Refreshing`, `Service`, and `No data`.
    - Running `pnpm --filter gnome-extension test -- test/view-model.test.js` fails before implementation because the new assertions are stricter than the existing contract.
  </acceptance_criteria>
  <action>Rewrite `apps/gnome-extension/test/view-model.test.js` first so it locks the Phase 7 contract from D-01 through D-12 and the `07-UI-SPEC.md` indicator/menu tables. Add failing assertions for indicator labels `Refreshing`, `{healthyCount}/{providerCount} ok`, `{issueCount} issue(s)`, `Service`, and `No data`. Add failing provider-row assertions for `iconKey`, `statusIconName`, `quotaText`, `progressPercent`, `progressVisible`, `secondaryText`, `issueSummaryText`, `detailsSuggestedCommandText`, and the absence of inline `Suggested command:` or `Source:` text in the row-facing fields.</action>
  <verify>
    <automated>pnpm --filter gnome-extension test -- test/view-model.test.js</automated>
  </verify>
  <done>The test file expresses the new compact UI contract in executable assertions and fails against the old implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the compact view-model contract and make the tests pass</name>
  <read_first>
    apps/gnome-extension/utils/view-model.js
    apps/gnome-extension/test/view-model.test.js
    apps/gnome-extension/utils/time.js
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-RESEARCH.md
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
  </read_first>
  <files>apps/gnome-extension/utils/view-model.js</files>
  <acceptance_criteria>
    - `apps/gnome-extension/utils/view-model.js` contains `healthyCount`, `issueCount`, `progressPercent`, `progressVisible`, `secondaryText`, and `detailsSuggestedCommandText`.
    - `apps/gnome-extension/utils/view-model.js` contains the exact indicator label strings `Refreshing`, `Service`, and `No data`.
    - `apps/gnome-extension/utils/view-model.js` normalizes provider status labels to concise text and keeps `Suggested command:` text out of the primary row fields.
    - `pnpm --filter gnome-extension test -- test/view-model.test.js` passes after implementation.
  </acceptance_criteria>
  <action>Update `apps/gnome-extension/utils/view-model.js` to expose a rendering contract that matches the tests. Keep the normalized snapshot seam intact per D-03 and Phase 4 patterns. Build aggregate counts for `healthyCount`, `issueCount`, and `providerCount`; implement indicator labels exactly as `Refreshing`, `{healthyCount}/{providerCount} ok`, `{issueCount} issue` or `{issueCount} issues`, `Service`, and `No data` per D-09 and the UI spec. Expand each provider row to include `iconKey`, `statusIconName`, `accentColor`, `quotaText`, `progressPercent` clamped `0-100`, `progressVisible`, `secondaryText`, `issueSummaryText`, `detailsSourceText`, and `detailsSuggestedCommandText`. Use concise labels such as `Reset Tomorrow`, `Missing secret-tool`, and `Auth needed`; keep verbose diagnostics and commands out of the primary row fields. Preserve provider order and existing refresh/back-end error surfaces in the snapshot-level details data.</action>
  <verify>
    <automated>pnpm --filter gnome-extension test -- test/view-model.test.js</automated>
  </verify>
  <done>The view-model exports a compact, tested contract for the redesigned indicator/menu, and the Node-side regression suite proves the UI can render from normalized data without inline text noise.</done>
</task>

</tasks>

<verification>
Use the RED then GREEN test run from the two tasks. The plan is complete only when the final targeted Vitest command passes with the new compact contract assertions.
</verification>

<success_criteria>
Indicator and provider-row data is fully prepared for the redesigned UI: aggregate-only top-bar labels, compact row fields, restrained secondary metadata, and details-only commands/diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-02-SUMMARY.md`
</output>
