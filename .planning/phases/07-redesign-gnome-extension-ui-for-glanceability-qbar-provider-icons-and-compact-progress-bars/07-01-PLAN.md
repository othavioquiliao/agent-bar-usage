---
phase: 07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/gnome-extension/stylesheet.css
  - apps/gnome-extension/assets/claude-code-icon.png
  - apps/gnome-extension/assets/codex-icon.png
  - apps/gnome-extension/utils/provider-icons.js
  - apps/gnome-extension/extension.js
  - scripts/install-ubuntu.sh
  - scripts/verify-gnome-wave0.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "Packaged GNOME extension installs with its own stylesheet and provider icons instead of depending on repo-relative qbar paths."
    - "Provider identity assets are available to the menu after install for Claude, Codex, and a neutral Copilot fallback."
    - "The redesign's neutral surfaces and restrained accent tokens exist in a shell stylesheet before row rendering starts."
    - "Wave 0 closes the install-flow payload assertion and GNOME smoke prerequisites before visual verification begins."
  artifacts:
    - path: "apps/gnome-extension/stylesheet.css"
      provides: "Shell-only classes for summary rows, provider rows, progress bars, and details rows"
      contains: ".agent-bar-ubuntu-provider-row"
    - path: "apps/gnome-extension/utils/provider-icons.js"
      provides: "Packaged icon and fallback badge helper for provider identity"
      exports: ["createProviderIdentityActor"]
    - path: "scripts/install-ubuntu.sh"
      provides: "Installed extension copy flow that includes stylesheet.css and assets/"
      contains: "stylesheet.css"
    - path: "scripts/verify-gnome-wave0.sh"
      provides: "Wave 0 preflight for install payload assertions and GNOME smoke prerequisites"
      contains: "gnome-shell"
  key_links:
    - from: "scripts/install-ubuntu.sh"
      to: "apps/gnome-extension/assets"
      via: "extension copy loop includes assets"
      pattern: "stylesheet\\.css|assets"
    - from: "apps/gnome-extension/extension.js"
      to: "apps/gnome-extension/stylesheet.css"
      via: "theme stylesheet load/unload during enable and disable"
      pattern: "load_stylesheet|unload_stylesheet"
---

<objective>
Create the packaged visual foundation for the GNOME redesign: a real extension stylesheet, bundled provider assets, a provider identity helper, and install/runtime wiring that works outside the repo checkout.

Purpose: Phase 7 cannot ship glanceable rows per D-04, D-07, and D-08 until the extension owns its CSS and icon assets.
Output: `stylesheet.css`, bundled provider assets, `provider-icons.js`, install/runtime wiring, and a Wave 0 preflight command that closes Nyquist gaps before live GNOME verification.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-CONTEXT.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-RESEARCH.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-VALIDATION.md
@/home/othavio/Work/agent-bar-usage/.impeccable.md
@[apps/gnome-extension/extension.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/extension.js)
@[scripts/install-ubuntu.sh](/home/othavio/Work/agent-bar-usage/scripts/install-ubuntu.sh)
@[qbar/src/theme.ts](/home/othavio/Work/agent-bar-usage/qbar/src/theme.ts)
@[qbar/snippets/waybar-style.css](/home/othavio/Work/agent-bar-usage/qbar/snippets/waybar-style.css)

<interfaces>
Current extension install/runtime anchors:

```js
// apps/gnome-extension/extension.js
export default class AgentBarUbuntuExtension extends Extension {
  enable() { ... }
  disable() { ... }
}
```

```bash
# scripts/install-ubuntu.sh
for item in extension.js metadata.json panel services state utils; do
  cp -r "$gnome_ext_src/$item" "$gnome_ext_dir/"
done
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add packaged stylesheet and provider identity assets</name>
  <read_first>
    apps/gnome-extension/extension.js
    qbar/icons/claude-code-icon.png
    qbar/icons/codex-icon.png
    qbar/src/theme.ts
    qbar/snippets/waybar-style.css
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-CONTEXT.md
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
  </read_first>
  <files>apps/gnome-extension/stylesheet.css, apps/gnome-extension/assets/claude-code-icon.png, apps/gnome-extension/assets/codex-icon.png, apps/gnome-extension/utils/provider-icons.js</files>
  <acceptance_criteria>
    - `apps/gnome-extension/stylesheet.css` contains `.agent-bar-ubuntu-summary-row`, `.agent-bar-ubuntu-provider-row`, `.agent-bar-ubuntu-progress`, and `.agent-bar-ubuntu-details`.
    - `apps/gnome-extension/stylesheet.css` contains the fallback tokens `#1f2329`, `#242a33`, `#d19a66`, `#98c379`, `#61afef`, and `#e06c75`.
    - `apps/gnome-extension/utils/provider-icons.js` exports `createProviderIdentityActor` and contains `claude-code-icon.png`, `codex-icon.png`, and `copilot` fallback handling without any `qbar/` runtime path.
    - `apps/gnome-extension/assets/claude-code-icon.png` and `apps/gnome-extension/assets/codex-icon.png` exist inside `apps/gnome-extension/assets/`.
  </acceptance_criteria>
  <action>Create the packaged style and icon layer per D-05, D-07, and D-08. Copy `qbar/icons/claude-code-icon.png` and `qbar/icons/codex-icon.png` into `apps/gnome-extension/assets/`. Add `apps/gnome-extension/stylesheet.css` with shell classes for a neutral summary row, compact provider rows, 24px icon slot, 4px progress track/fill, muted details rows, and a restrained hover surface using the UI-SPEC fallback values `#1f2329`, `#242a33`, `rgba(192, 201, 212, 0.04)`, Claude `#d19a66`, Codex `#98c379`, Copilot `#61afef`, and error `#e06c75`. Create `apps/gnome-extension/utils/provider-icons.js` exporting `createProviderIdentityActor({ extension, providerId, status, size = 16 })`; it must load packaged Claude/Codex icons through `Gio.FileIcon` and return a neutral readable Copilot fallback badge actor with the label `Co` rather than using a symbolic-only icon. Do not reference `qbar/` at runtime.</action>
  <verify>
    <automated>rg -n "agent-bar-ubuntu-summary-row|agent-bar-ubuntu-provider-row|agent-bar-ubuntu-progress|agent-bar-ubuntu-details|#1f2329|#242a33|#d19a66|#98c379|#61afef|#e06c75" apps/gnome-extension/stylesheet.css apps/gnome-extension/utils/provider-icons.js</automated>
  </verify>
  <done>The extension source tree owns its visual primitives and provider identity assets, and those artifacts no longer depend on the `qbar/` checkout being present at runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stylesheet and assets into extension runtime and install flow</name>
  <read_first>
    apps/gnome-extension/extension.js
    scripts/install-ubuntu.sh
    apps/gnome-extension/stylesheet.css
    apps/gnome-extension/utils/provider-icons.js
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-RESEARCH.md
  </read_first>
  <files>apps/gnome-extension/extension.js, scripts/install-ubuntu.sh</files>
  <acceptance_criteria>
    - `apps/gnome-extension/extension.js` contains `load_stylesheet` and `unload_stylesheet` calls around a `stylesheet.css` file resolved from the extension directory.
    - `scripts/install-ubuntu.sh` copies `stylesheet.css` and `assets` into `${HOME}/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev`.
    - `scripts/install-ubuntu.sh` still copies `panel`, `services`, `state`, and `utils`, so the redesign does not regress the existing extension payload.
  </acceptance_criteria>
  <action>Update `apps/gnome-extension/extension.js` so `enable()` resolves `this.dir.get_child("stylesheet.css")`, loads it through the GNOME Shell theme API before adding the indicator, and `disable()` unloads the same stylesheet if it was loaded. Preserve the existing lifecycle discipline from research: no shell mutations at module initialization and explicit cleanup on disable. Update `scripts/install-ubuntu.sh` so the extension copy loop includes `stylesheet.css` and `assets` alongside `extension.js`, `metadata.json`, `panel`, `services`, `state`, and `utils`. Do not add repo-relative fallback logic for assets; the installed extension directory is the only supported runtime source.</action>
  <verify>
    <automated>rg -n "load_stylesheet|unload_stylesheet|stylesheet\\.css|assets|panel|services|state|utils" apps/gnome-extension/extension.js scripts/install-ubuntu.sh</automated>
  </verify>
  <done>The install path ships the new assets, and the extension lifecycle loads and unloads the stylesheet explicitly so packaged visuals work in installed GNOME sessions.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Wave 0 preflight command for install payload and GNOME smoke prerequisites</name>
  <read_first>
    scripts/install-ubuntu.sh
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-VALIDATION.md
    apps/gnome-extension/utils/provider-icons.js
    apps/gnome-extension/extension.js
  </read_first>
  <files>scripts/verify-gnome-wave0.sh</files>
  <acceptance_criteria>
    - `scripts/verify-gnome-wave0.sh` contains `stylesheet.css`, `assets/claude-code-icon.png`, and `assets/codex-icon.png` install-payload assertions under `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev`.
    - `scripts/verify-gnome-wave0.sh` checks `pnpm`, `gjs`, `gnome-shell`, and `gnome-extensions` availability with actionable failure text.
    - `scripts/verify-gnome-wave0.sh` checks that packaged icon loading does not depend on a repo-relative `qbar/` fallback.
  </acceptance_criteria>
  <action>Create `scripts/verify-gnome-wave0.sh` as the explicit Wave 0 closer referenced by `07-VALIDATION.md`. The script must: assert `command -v pnpm`, `command -v gjs`, `command -v gnome-shell`, and `command -v gnome-extensions`; inspect the installed extension directory `"$HOME/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev"` for `stylesheet.css`, `assets/claude-code-icon.png`, and `assets/codex-icon.png`; and grep the installed or source `apps/gnome-extension/utils/provider-icons.js` helper to ensure it does not contain a repo-relative `qbar/` runtime fallback. Print explicit remediation text for any missing prerequisite so Wave 2 can stop before manual GNOME verification if the environment is not ready.</action>
  <verify>
    <automated>bash scripts/verify-gnome-wave0.sh</automated>
  </verify>
  <done>Wave 0 provides a concrete automated gate for packaged install payload and GNOME smoke prerequisites, so later plans no longer rely on `MISSING` verification placeholders.</done>
</task>

</tasks>

<verification>
Run the task-level grep checks, then `bash scripts/verify-gnome-wave0.sh` to confirm the extension source tree and installed extension payload are ready for later live GNOME verification.
</verification>

<success_criteria>
The extension package owns its CSS and provider assets, install/runtime wiring guarantees those files are present and loaded in installed GNOME sessions, and Wave 0 closes the install/preflight Nyquist gaps before visual verification begins.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-01-SUMMARY.md`
</output>
