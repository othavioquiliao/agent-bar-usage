---
phase: 07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars
plan: "03"
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - apps/gnome-extension/panel/provider-row-model.js
  - apps/gnome-extension/panel/progress-bar.js
  - apps/gnome-extension/panel/provider-row.js
  - apps/gnome-extension/panel/menu-builder.js
  - apps/gnome-extension/panel/indicator.js
  - apps/gnome-extension/test/provider-row.test.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "Users can scan the expanded menu in one open-close cycle and see provider identity, quota progress, and a short status line without reading paragraphs."
    - "The top-bar indicator stays calm and aggregate-only while still showing meaningful state beyond a generic provider count."
    - "Refresh action gating, details footer visibility, and error readability survive the redesign."
  artifacts:
    - path: "apps/gnome-extension/panel/provider-row-model.js"
      provides: "Pure row-composition seam for provider-row layout decisions"
      exports: ["buildProviderRowLayoutModel"]
    - path: "apps/gnome-extension/panel/progress-bar.js"
      provides: "Reusable compact progress-bar actor for 4px quota tracks"
      exports: ["createProgressBar"]
    - path: "apps/gnome-extension/panel/provider-row.js"
      provides: "Structured provider row actors built from the compact view-model contract"
      exports: ["createProviderRow"]
    - path: "apps/gnome-extension/panel/menu-builder.js"
      provides: "Summary row, provider list, details area, and refresh action hierarchy"
      exports: ["rebuildMenu"]
    - path: "apps/gnome-extension/panel/indicator.js"
      provides: "Aggregate-only top-bar indicator that keeps the Phase 7 summary contract"
      exports: ["Indicator"]
  key_links:
    - from: "apps/gnome-extension/panel/provider-row.js"
      to: "apps/gnome-extension/panel/provider-row-model.js"
      via: "pure layout helper consumed before GJS actor creation"
      pattern: "buildProviderRowLayoutModel"
    - from: "apps/gnome-extension/panel/provider-row.js"
      to: "apps/gnome-extension/panel/progress-bar.js"
      via: "compact quota bar rendered inside the provider row"
      pattern: "createProgressBar"
    - from: "apps/gnome-extension/panel/provider-row.js"
      to: "apps/gnome-extension/utils/provider-icons.js"
      via: "identity actor helper for icon slot"
      pattern: "createProviderIdentityActor"
    - from: "apps/gnome-extension/panel/menu-builder.js"
      to: "apps/gnome-extension/utils/view-model.js"
      via: "snapshot view-model driven menu rebuild"
      pattern: "buildSnapshotViewModel"
    - from: "apps/gnome-extension/panel/indicator.js"
      to: "apps/gnome-extension/utils/view-model.js"
      via: "aggregate icon and label state"
      pattern: "buildIndicatorSummaryViewModel"
---

<objective>
Render the redesigned GNOME indicator and popup menu using the new compact data contract, bundled icons, and stylesheet classes, then pause for live GNOME verification.

Purpose: This is the user-visible payoff of Phase 7 and must preserve Phase 4 refresh behavior while replacing prose-heavy rows with structured GNOME widgets.
Output: Structured menu rows, compact progress bars, aggregate indicator copy, and a live validation gate for GNOME Shell 46.
</objective>

<execution_context>
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/workflows/execute-plan.md
@/home/othavio/Work/agent-bar-usage/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-CONTEXT.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-RESEARCH.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
@.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-VALIDATION.md
@.planning/phases/04-ubuntu-desktop-surface/04-03-SUMMARY.md
@[apps/gnome-extension/panel/indicator.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/panel/indicator.js)
@[apps/gnome-extension/panel/menu-builder.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/panel/menu-builder.js)
@[apps/gnome-extension/panel/provider-row.js](/home/othavio/Work/agent-bar-usage/apps/gnome-extension/panel/provider-row.js)

<interfaces>
Available rendering seam from prior plans:

```js
// from 07-01
export function createProviderIdentityActor({ extension, providerId, status, size = 16 }) { ... }
```

```js
// from 07-02
const providerRow = {
  providerId,
  title,
  statusText,
  statusIconName,
  iconKey,
  quotaText,
  progressPercent,
  progressVisible,
  secondaryText,
  issueSummaryText,
  detailsSuggestedCommandText,
};
```

Current menu entrypoint:

```js
export function rebuildMenu(menu, state, { onRefresh = null, now = new Date() } = {}) { ... }
```

Pure helper seam required for Node-side regression coverage:

```js
export function buildProviderRowLayoutModel(viewModel) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace multiline provider rows with structured St widget composition</name>
  <read_first>
    apps/gnome-extension/panel/provider-row.js
    apps/gnome-extension/panel/provider-row-model.js
    apps/gnome-extension/panel/menu-builder.js
    apps/gnome-extension/utils/provider-icons.js
    apps/gnome-extension/utils/view-model.js
    apps/gnome-extension/stylesheet.css
    apps/gnome-extension/test/provider-row.test.js
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
  </read_first>
  <files>apps/gnome-extension/panel/provider-row-model.js, apps/gnome-extension/panel/progress-bar.js, apps/gnome-extension/panel/provider-row.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/test/provider-row.test.js</files>
  <acceptance_criteria>
    - `apps/gnome-extension/panel/provider-row-model.js` exports `buildProviderRowLayoutModel` without GJS imports so Vitest can run it in Node.
    - `apps/gnome-extension/panel/provider-row.js` uses `PopupBaseMenuItem` and `St` widgets instead of `formatRowLines`.
    - `apps/gnome-extension/panel/provider-row.js` contains `buildProviderRowLayoutModel`, `createProviderIdentityActor`, and `createProgressBar`.
    - `apps/gnome-extension/panel/menu-builder.js` contains `Summary`, `Details`, and `Refresh Now` or `Refreshing...` text without building a `summaryTitle + summaryBody` multiline block.
    - `apps/gnome-extension/panel/menu-builder.js` preserves `refreshItem.sensitive = !state.isLoading`.
    - `apps/gnome-extension/test/provider-row.test.js` contains assertions for secondary-line selection, progress visibility, and no inline `Suggested command:` text in the primary row layout model.
  </acceptance_criteria>
  <action>Create `apps/gnome-extension/panel/provider-row-model.js` as a pure JS seam exporting `buildProviderRowLayoutModel(viewModel)` so Node-side tests can cover non-trivial row decisions without importing GJS modules. That helper must decide the primary quota line, whether the progress bar should be shown, and which single secondary line wins when reset text, issue text, or metadata compete. Add `apps/gnome-extension/test/provider-row.test.js` covering secondary-line prioritization, progress visibility, and exclusion of inline `Suggested command:` text from the primary row model. Create `apps/gnome-extension/panel/progress-bar.js` exporting `createProgressBar({ percent, accentClass, showFill })`, implemented with simple `St.Widget` or `St.Bin` track/fill actors that set explicit `x_expand` so GNOME 46 stretches the 4px bar correctly. Rewrite `apps/gnome-extension/panel/provider-row.js` per D-01, D-02, D-04, D-06, D-07, and D-10 to consume `buildProviderRowLayoutModel(viewModel)`, then render `PopupMenu.PopupBaseMenuItem`, a 24px left icon slot via `createProviderIdentityActor`, a 14px/600 header label, a 12px status label with optional symbolic glyph, a compact quota line, the progress bar directly below it, and one short secondary line only. Rewrite `apps/gnome-extension/panel/menu-builder.js` so the top of the menu is one concise summary row, followed by provider rows in backend/config order, then a visually secondary details area and the final refresh action. Remove the old `Status` prose block, remove inline `Suggested command:` text from provider rows, and keep `refreshItem.sensitive = !state.isLoading` unchanged per D-11 and D-12.</action>
  <verify>
    <automated>pnpm --filter gnome-extension exec vitest run test/provider-row.test.js test/polling-service.test.js --config vitest.config.ts</automated>
  </verify>
  <done>The popup menu becomes structured and scan-friendly: summary row first, compact provider rows second, details footer third, and the existing single-flight refresh action last.</done>
</task>

<task type="auto">
  <name>Task 2: Update the top-bar indicator to the aggregate-only Phase 7 summary contract</name>
  <read_first>
    apps/gnome-extension/panel/indicator.js
    apps/gnome-extension/utils/view-model.js
    apps/gnome-extension/panel/menu-builder.js
    apps/gnome-extension/stylesheet.css
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
  </read_first>
  <files>apps/gnome-extension/panel/indicator.js</files>
  <acceptance_criteria>
    - `apps/gnome-extension/panel/indicator.js` still calls `buildIndicatorSummaryViewModel(this._state)` and `rebuildMenu(this.menu, this._state, { onRefresh: this._refreshHandler })`.
    - `apps/gnome-extension/panel/indicator.js` does not add provider-specific icons or per-provider strips to the panel button.
    - `apps/gnome-extension/panel/indicator.js` still toggles `_label.visible` based on the compact aggregate label text.
  </acceptance_criteria>
  <action>Keep `apps/gnome-extension/panel/indicator.js` aggregate-only per D-09. Continue rendering a single panel icon plus one short label using the view-model output from 07-02: `Refreshing`, `{healthyCount}/{providerCount} ok`, `{issueCount} issue(s)`, `Service`, or `No data`. Do not add provider icons, progress bars, or multiline copy to the top bar. Preserve the current rebuild pattern so menu updates still happen from the same state change path.</action>
  <verify>
    <automated>rg -n "buildIndicatorSummaryViewModel|rebuildMenu|_label\\.visible|system-status-icon" apps/gnome-extension/panel/indicator.js</automated>
  </verify>
  <done>The top bar remains calm and compact while surfacing a more informative aggregate state than the old generic provider count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the redesigned menu in a real GNOME Shell 46 session</name>
  <read_first>
    apps/gnome-extension/extension.js
    scripts/install-ubuntu.sh
    apps/gnome-extension/stylesheet.css
    apps/gnome-extension/panel/indicator.js
    apps/gnome-extension/panel/menu-builder.js
    apps/gnome-extension/panel/provider-row.js
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-UI-SPEC.md
    .planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-VALIDATION.md
  </read_first>
  <files>apps/gnome-extension/extension.js, scripts/install-ubuntu.sh, apps/gnome-extension/stylesheet.css, apps/gnome-extension/panel/indicator.js, apps/gnome-extension/panel/menu-builder.js, apps/gnome-extension/panel/provider-row.js</files>
  <acceptance_criteria>
    - `bash scripts/verify-gnome-wave0.sh --source-only` passes before install.
    - `pnpm install:ubuntu` completes before any check that inspects `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev`.
    - `bash scripts/verify-gnome-wave0.sh --post-install` passes before the live checklist starts.
    - Human verification response explicitly says `approved` or lists concrete defects tied to indicator state, provider rows, details area, or refresh gating.
    - Verification steps cover healthy, issue, loading, no-data or backend-failure states, installed-extension icon resolution, and GNOME 46 progress-bar expansion/collapse behavior as available on the user's machine.
  </acceptance_criteria>
  <action>Run the live GNOME verification pass only after the ordered sequence `bash scripts/verify-gnome-wave0.sh --source-only`, `pnpm install:ubuntu`, and `bash scripts/verify-gnome-wave0.sh --post-install` succeeds. Use the installed extension produced by `pnpm install:ubuntu`, inspect the indicator and menu against the exact UI-SPEC and `07-VALIDATION.md` manual-only checks, and stop execution until the user replies with `approved` or a defect list.</action>
  <what-built>Packaged stylesheet/assets, compact provider rows with icons and progress bars, concise details footer, and aggregate-only top-bar copy.</what-built>
  <how-to-verify>
    1. Run `bash scripts/verify-gnome-wave0.sh --source-only` and fix any reported prerequisite or source-tree failures before continuing.
    2. Run `pnpm install:ubuntu`.
    3. Run `bash scripts/verify-gnome-wave0.sh --post-install`.
    4. Confirm `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev/` now contains `stylesheet.css`, `assets/claude-code-icon.png`, and `assets/codex-icon.png`.
    5. Restart GNOME Shell or log out/in if the extension does not reload automatically.
    6. Confirm the panel indicator shows one icon plus one short label only, never a provider strip.
    7. Open the menu and confirm each provider row shows icon, name, status, quota/progress, and one short secondary line instead of multiline prose.
    8. Validate installed-extension icon resolution by confirming Claude and Codex icons still render from `~/.local/share/gnome-shell/extensions/agent-bar-ubuntu@othavio.dev/assets/` and not from a repo-relative `qbar/` path.
    9. Validate GNOME 46 progress-bar expansion by checking one healthy provider row and one degraded/error row: the 4px bar stretches across the content column when quota data exists and does not collapse or overflow.
    10. Trigger a refresh and confirm the action label changes to `Refreshing...` and stays disabled while loading.
    11. If any provider is degraded or unavailable, confirm the row still reads clearly without relying on color alone and the actionable command appears in Details, not inline in the provider row.
  </how-to-verify>
  <verify>
    <automated>pnpm install:ubuntu && bash scripts/verify-gnome-wave0.sh --post-install</automated>
  </verify>
  <resume-signal>Type `approved` to continue, or reply with the specific UI defects to fix.</resume-signal>
  <done>The redesign is either approved in a real GNOME session or returns a concrete defect list for follow-up work.</done>
</task>

</tasks>

<verification>
Run `pnpm --filter gnome-extension exec vitest run test/provider-row.test.js test/polling-service.test.js --config vitest.config.ts` to cover both row-composition behavior and D-12 refresh gating, then `bash scripts/verify-gnome-wave0.sh --source-only`, then `pnpm install:ubuntu && bash scripts/verify-gnome-wave0.sh --post-install`, then stop at the GNOME Shell checkpoint. The phase is not considered visually verified until the user confirms the live session behavior.
</verification>

<success_criteria>
The indicator and expanded menu match the Phase 7 UI contract in live GNOME use: aggregate top-bar summary, icon-led provider rows, compact progress bars, terse details, and preserved single-flight refresh behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-redesign-gnome-extension-ui-for-glanceability-qbar-provider-icons-and-compact-progress-bars/07-03-SUMMARY.md`
</output>
