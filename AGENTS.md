# Workspace Guide

This workspace uses GSD workflows for project planning and execution.

- Start substantive repo changes through the appropriate `gsd-*` workflow so `.planning/` stays current.
- Use [CLAUDE.md](/home/othavio/Work/agent-bar-usage/CLAUDE.md) as the generated project guide and condensed context source.
- Use `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` as the source of truth for project scope and progress.

This file exists as a compatibility guide for tooling that expects a root `AGENTS.md`.
